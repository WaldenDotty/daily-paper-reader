---
title: Visual SLAM with DEM Anchoring for Lunar Surface Navigation
title_zh: 基于 DEM 锚定的月球表面导航视觉 SLAM
authors: "Adam Dai, Guillem Casadesus Vila, Grace Gao"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17229v1"
tags: ["query:cislunar-dyn-nav"]
score: 9.0
evidence: 结合数字高程模型锚定的月面导航视觉SLAM
tldr: 针对月球表面缺乏全球定位系统、光照极端且纹理稀疏导致的视觉里程计漂移问题，本文提出一种结合数字高程模型（DEM）锚定的立体视觉SLAM系统。该系统前端利用深度学习进行特征提取与匹配以增强鲁棒性，后端将DEM提供的高度和表面法线约束引入位姿图优化。实验证明，该方法在模拟和类月模拟场地中均能显著降低长距离导航的累积误差。
selection_source: fresh_fetch
motivation: 月球表面环境极端且缺乏全球定位信号，导致传统视觉导航系统在长距离行驶中存在严重的累积漂移。
method: 采用基于学习的特征匹配前端，并在后端位姿图中整合来自数字高程模型（DEM）的高度和表面法线约束。
result: 在Unreal Engine模拟数据和埃特纳火山类月模拟实验中，该方法相比基准SLAM显著降低了绝对轨迹误差。
conclusion: 通过引入DEM作为绝对表面约束，该系统有效抑制了月球漫游车在复杂地形下的长距离导航漂移。
---

## 摘要
未来的月球任务将需要自主漫游车能够在挑战性地形中穿越数十公里，同时保持精确的定位并生成全局一致的地图。然而，由于缺乏全球定位系统、极端的照明条件以及低纹理的月壤，月球上的长距离导航变得尤为困难，因为视觉惯性里程计流水线在长距离穿越过程中会积累漂移。为了应对这一挑战，我们提出了一种双目视觉同步定位与地图构建 (SLAM) 系统，该系统将基于学习的特征检测与匹配与来自数字高程模型 (DEM) 的全局约束相结合。我们的前端采用基于学习的特征提取和匹配，以实现对极端照明和重复地形的鲁棒性；而后端则将源自 DEM 的高度和表面法线因子整合到位姿图中，提供绝对表面约束，从而减轻长期漂移。我们使用在虚幻引擎 (Unreal Engine) 中生成的模拟月球穿越数据以及从埃特纳火山 (Mt. Etna) 采集的真实月球/火星类比数据验证了我们的方法。结果表明，与基准 SLAM 方法相比，DEM 锚定一致地减少了绝对轨迹误差，即使在重复或视觉混淆的地形中也能降低长距离导航的漂移。

## Abstract
Future lunar missions will require autonomous rovers capable of traversing tens of kilometers across challenging terrain while maintaining accurate localization and producing globally consistent maps. However, the absence of global positioning systems, extreme illumination, and low-texture regolith make long-range navigation on the Moon particularly difficult, as visual-inertial odometry pipelines accumulate drift over extended traverses. To address this challenge, we present a stereo visual simultaneous localization and mapping (SLAM) system that integrates learned feature detection and matching with global constraints from digital elevation models (DEMs). Our front-end employs learning-based feature extraction and matching to achieve robustness to illumination extremes and repetitive terrain, while the back-end incorporates DEM-derived height and surface-normal factors into a pose graph, providing absolute surface constraints that mitigate long-term drift. We validate our approach using both simulated lunar traverse data generated in Unreal Engine and real Moon/Mars analog data collected from Mt. Etna. Results demonstrate that DEM anchoring consistently reduces absolute trajectory error compared to baseline SLAM methods, lowering drift in long-range navigation even in repetitive or visually aliased terrain.

---

## 论文详细总结（自动生成）

### 1. 核心问题与研究动机
论文关注的核心问题是**月球表面长距离（数十至数千公里）自主导航中的定位漂移问题**。
*   **背景**：月球缺乏全球卫星导航系统（GNSS），且环境极端（光照剧烈变化、月壤纹理稀疏且重复）。
*   **痛点**：传统的视觉里程计（VO）或视觉惯性里程计（VIO）在长距离行驶中会产生累积误差（漂移）。由于月面地形单调，往往缺乏闭环检测（Loop Closure）的机会，导致无法纠正全局误差。
*   **动机**：利用已有的轨道观测数据——数字高程模型（DEM）作为全局先验，通过将其引入 SLAM 系统来提供绝对高度和姿态约束，从而抑制漂移。

### 2. 方法论
论文提出了一种结合深度学习前端与 DEM 锚定后端的立体视觉 SLAM 系统：
*   **前端（特征提取与匹配）**：
    *   采用 **SuperPoint** 进行特征点检测，利用 **LightGlue**（基于 Transformer）进行特征匹配。这种组合比传统算子（如 ORB）对极端光照和视角变化更具鲁棒性。
    *   通过双目视觉三角化获取 3D 地标，并求解 PnP 问题估计帧间相对位姿。
*   **后端（位姿图优化与 DEM 锚定）**：
    *   在标准的位姿图中引入了两种基于 DEM 的约束因子：
        1.  **高度因子（Height Factor）**：将漫游车当前的水平位置 $(x, y)$ 在 DEM 中进行双线性插值，获取参考高度 $\hat{z}$，计算估计高度与参考高度的残差 $r_{height} = z_i - \hat{z}_i$。
        2.  **表面法线因子（Surface Normal Factor）**：利用 DEM 的梯度计算局部表面法线 $n_{DEM}$，并将其与漫游车估计的本体法线 $n_i$ 进行对齐，约束漫游车的俯仰角（Pitch）和横滚角（Roll）。
    *   使用 **GTSAM** 库进行非线性最小二乘优化（Levenberg-Marquardt 算法）。

### 3. 实验设计
论文在三种不同性质的数据集上进行了验证：
*   **LuSNAR 数据集**：合成的月球多传感器基准数据集，包含 9 个场景（200-500 米）。
*   **S3LI 数据集**：在意大利埃特纳火山（Mt. Etna）采集的真实类月/类火星模拟数据，包含 1.03 公里的环形火山口路径，具有严重的视觉混淆。
*   **Unreal Engine 5 模拟器**：基于真实的 LOLA（月球轨道激光高度计）数据构建，进行了长达 **11 公里** 的长距离穿越实验。
*   **对比基准（Benchmark）**：主要对比了 **ORB-SLAM3**（立体模式）以及不带 DEM 约束的基准视觉里程计（VO）。

### 4. 资源与算力
*   **软件环境**：使用了 Unreal Engine 5、Python、C++ 和 GTSAM 优化库。
*   **模型权重**：特征提取（SuperPoint）和匹配（LightGlue）直接使用了官方预训练权重，未进行额外的微调。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号、数量或推理/训练时长。但提到系统设计目标是实时运行。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 LuSNAR 的 9 个场景中进行了定量对比。
    *   在 S3LI 真实数据上进行了 1 公里级的闭环实验。
    *   在 UE5 中进行了 11 公里的超长距离实验。
    *   进行了 **DEM 不确定性分析**（消融实验），测试了在存在垂直噪声、垂直偏差和水平偏移情况下的鲁棒性。
*   **充分性评价**：实验设计较为全面，涵盖了从仿真到真实模拟场地的跨越，且通过注入受控噪声验证了系统的边界，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **精度提升**：引入 DEM 锚定后，绝对轨迹误差（ATE）显著降低。在 1.19 公里的仿真实验中，ATE 从 78.0 米降至 7.71 米（提升了一个数量级）。
*   **局部稳定性**：DEM 约束主要作用于全局一致性，不会损害局部相对位姿误差（RPE），即保证了轨迹的平滑性。
*   **正则化作用**：在 S3LI 实验中发现，DEM 锚定能有效正则化闭环检测，防止在漂移过大时闭环优化导致轨迹发生物理上不可能的畸变。
*   **鲁棒性**：系统对中等程度的 DEM 误差（如 1-2 米的噪声或偏移）具有良好的容忍度。

### 7. 优点
*   **多源融合**：巧妙地将轨道遥感数据（DEM）与漫游车局部感知（视觉 SLAM）结合，解决了无 GNSS 环境下的绝对定位参考问题。
*   **前端鲁棒性**：采用学习型特征匹配，解决了月面极端光照导致的传统特征点失效问题。
*   **实用性强**：DEM 数据在月球探索中是现成可用的，该方法不需要额外部署基础设施。

### 8. 不足与局限
*   **水平约束较弱**：DEM 主要提供高度和倾角约束，对于在平坦、视觉重复地形上的水平（切向）漂移约束能力有限。
*   **计算资源限制**：在 11 公里的长距离实验中，论文提到后端资源限制导致轨迹重建提前终止，说明在大规模位姿图管理上仍有优化空间。
*   **依赖 DEM 精度**：虽然做了不确定性分析，但在地形极度平坦或 DEM 分辨率极低的情况下，该方法的增益会减弱。

（完）
