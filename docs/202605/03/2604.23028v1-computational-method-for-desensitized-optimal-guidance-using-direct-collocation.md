---
title: Computational Method for Desensitized Optimal Guidance Using Direct Collocation
title_zh: 基于直接配点法的去敏感最优制导计算方法
authors: "Katrina Winkler, Anil Rao"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.23028v1"
tags: ["query:cislunar-dyn-nav"]
score: 8.0
evidence: 减敏最优制导与轨迹优化
tldr: 本文提出了一种基于自适应高斯正交配点的去敏感最优制导计算方法。该方法通过在目标函数中增加状态对不确定参数的敏感度惩罚项，降低了动力学模型不确定性对轨迹的影响。利用Legendre-Gauss-Radau配点法在缩减时域内求解最优控制问题，并通过网格重映射和删除过期部分来提高效率。在Zermelo导航和大气再入问题的仿真中，该方法显著增强了对外部干扰和建模误差的鲁棒性，减小了终端误差。
selection_source: fresh_fetch
motivation: 旨在解决传统最优制导在面对动力学模型参数不确定性和外部干扰时鲁棒性不足的问题。
method: 通过在目标函数中引入敏感度惩罚项，并结合Legendre-Gauss-Radau配点法在缩减时域内进行实时轨迹优化。
result: 蒙特卡洛分析表明，该方法在不显著增加计算负担的前提下，实现了更紧凑的轨迹包络和更小的终端状态误差。
conclusion: 该去敏感最优制导方法能有效提升复杂动力学系统在不确定环境下的控制精度和鲁棒性。
---

## 摘要
本文开发了一种利用自适应高斯正交配点法进行去敏感最优制导的计算方法。该方法通过扩充目标泛函，显式地惩罚状态对不确定参数的灵敏度，从而计算出一条能够降低对动力学模型不确定性敏感度的参考轨迹。以该去敏感参考轨迹为起点，本文开发的去敏感最优制导方法在指定的制导更新时刻，计算剩余时域内的新最优控制。这一收缩时域最优控制问题采用 Legendre-Gauss-Radau 配点法求解。在每次制导更新时，通过将网格重新映射到剩余时域并删除与已过期时域相关的部分，来确定缩减时域的网格。结果表明，所得制导方案提高了对外部扰动和建模误差的鲁棒性。该方法在两个数值算例上进行了演示。第一个算例是 Zermelo 导航问题，展示了该方法在简单算例中的表现。第二个算例是大气再入问题，展示了该方法在更复杂问题中的性能。对于这两个算例，在动力学模型存在参数不确定性的情况下对动力学进行了仿真，并进行了蒙特卡洛分析。结果表明，与不惩罚灵敏度的方法相比，本文开发的方法在不显著增加计算负担的情况下，产生了更紧凑的轨迹包络和更小的终端状态误差。

## Abstract
A computational method is developed for desensitized optimal guidance using adaptive Gaussian quadrature collocation. The method computes a reference trajectory that reduces the sensitivity to uncertainties in the dynamic model by augmenting the objective functional to explicitly penalize the sensitivity of the state with respect to uncertain parameters. Using this desensitized reference trajectory as a starting point, the desensitized optimal guidance method developed in this paper computes a new optimal control on the remaining horizon at specified guidance update times. This shrinking horizon optimal control problem is solved using a Legendre-Gauss-Radau collocation method where, at each guidance update, a reduced-horizon mesh is determined by remapping the mesh to the remaining horizon and deleting the portion of the mesh associated with the expired portion of the horizon. The resulting guidance solution is found to improve robustness to external disturbances and modeling errors. The method is demonstrated on two numerical examples. The first example is Zermelo's navigation problem which illustrates the behavior of the method on a simple example. The second example is an atmospheric reentry problem that demonstrates the performance of the method on a more complex problem. For both examples, the dynamics are simulated in the presence of parameter uncertainties in the dynamic model, and Monte Carlo analysis is performed. The results show that the method developed in this paper produces tighter trajectory envelopes and smaller terminal state errors without significantly increasing the computational burden when compared with a method that does not penalize sensitivities.

---

## 论文详细总结（自动生成）

这篇论文由佛罗里达大学的 Katrina L. Winkler 和 Anil V. Rao 撰写，提出了一种结合去敏感最优控制（DOC）与实时制导更新的计算框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在实际航天或航空任务中，最优控制问题（OCP）通常基于名义动力学模型求解。然而，由于模型参数不确定性（如大气密度、气动系数）和外部干扰，实际执行轨迹往往会偏离名义轨迹。
*   **核心问题**：传统的制导方法（如轨迹跟踪或简单的在线重优化）未能在设计阶段显式考虑轨迹对参数变化的敏感性，导致在不确定性环境下鲁棒性不足，容易出现约束违反或终端误差过大。
*   **研究动机**：开发一种既能通过在线更新修正偏差，又能生成“天生”对参数误差不敏感的轨迹的制导方法，即**去敏感最优制导（DOG）**。

### 2. 方法论：核心思想与关键技术
该方法将去敏感控制理论嵌入到收缩时域（Shrinking-Horizon）制导框架中：
*   **去敏感建模**：将状态对不确定参数 $p$ 的敏感度（偏导数 $S = \partial x / \partial p$）作为增广状态变量引入动力学方程。
*   **目标函数扩充**：在原始性能指标（如时间、燃料或射程）的基础上，增加对终端敏感度和运行敏感度的惩罚项。通过权重系数 $\beta$ 平衡性能与鲁棒性。
*   **直接配点法（LGR）**：采用 $hp$ 自适应 Legendre-Gauss-Radau (LGR) 正交配点法，将连续时间的最优控制问题转化为离散的非线性规划（NLP）问题。
*   **网格重映射技术（关键效率提升）**：在每次制导更新时刻，算法不从零开始划分网格，而是删除已过期的网格部分，并将剩余网格重新映射到新的时间区间 $[-1, 1]$。这种方法利用了前一次计算的节点分布，显著加快了 NLP 求解器的收敛速度。

### 3. 实验设计
论文通过两个具有代表性的数值算例验证了方法：
*   **算例 1：Zermelo 导航问题**。一个简单的二维船只导航问题，其中河流流速参数存在不确定性。
*   **算例 2：可重复使用运载器（RLV）再入问题**。一个复杂的三维大气再入场景，考虑了大气密度标高（$H$）和零升阻力系数（$C_{D0}$）的随机扰动。
*   **Benchmark（基准）**：对比了不带敏感度惩罚（$\beta=0$）的标准最优制导方法。
*   **对比维度**：通过蒙特卡洛（Monte Carlo）分析，对比了不同去敏感权重 $\beta$ 下的终端状态误差分布、轨迹包络宽度和计算耗时。

### 4. 资源与算力
*   **硬件环境**：2025 Apple M3 Ultra Mac Studio（28 核 CPU，256 GB RAM）。
*   **软件工具**：MATLAB R2025a，使用 GPOPS-II 软件进行轨迹优化，IPOPT 作为 NLP 求解器，ADiGator 进行自动微分。
*   **计算耗时**：对于复杂的 RLV 再入问题，单次制导更新的平均 CPU 时间在 1-2 秒左右，证明了其在线应用的潜力。

### 5. 实验数量与充分性
*   **实验规模**：每个算例均进行了 1000 次蒙特卡洛试验，以确保统计结果的可靠性。
*   **消融与权衡研究**：进行了帕累托（Pareto）分析，展示了性能损失与鲁棒性增益之间的折中关系。
*   **充分性评价**：实验设计较为充分，不仅验证了精度提升，还通过改变制导更新频率（如 20s vs 40s）验证了该方法在低频更新下依然能保持高精度的特性。

### 6. 主要结论与发现
*   **显著降低离散度**：在 RLV 算例中，去敏感制导使终端经度的标准差降低了 82.85%，轨迹包络宽度缩减了约 45.89%。
*   **性能与鲁棒性的折中**：增加敏感度惩罚会略微降低名义性能（如射程减少 3%），但能换取巨大的鲁棒性提升。
*   **降低更新频率需求**：去敏感制导在更新频率减半（40s 一次）的情况下，其表现依然优于高频更新（20s 一次）的传统制导方法。

### 7. 优点
*   **算法效率高**：网格重映射策略巧妙地解决了在线重优化计算量大的痛点。
*   **鲁棒性强**：通过数学手段将不确定性直接建模在优化问题内，而非事后补偿。
*   **通用性**：基于直接配点法，易于推广到各种非线性约束的最优控制问题中。

### 8. 不足与局限
*   **状态维度爆炸**：引入敏感度变量会导致状态空间维度按 $n_x \times n_p$（状态数 $\times$ 参数数）增长，对于参数极多的复杂系统，计算负担可能显著增加。
*   **模型依赖性**：需要精确的动力学偏导数模型，如果物理模型本身存在结构性缺失（而非参数误差），该方法的效果可能受限。
*   **参数假设**：目前主要针对常值但未知的参数不确定性，对于快速时变的随机噪声处理能力尚待进一步验证。

（完）
