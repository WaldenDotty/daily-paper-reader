---
title: A practical guide to implementing zero-order-hold interplanetary trajectory legs
title_zh: 实现零阶保持器行星际轨道段的实用指南
authors: "Dario Izzo, Harry Holt, Giacomo Acciarini, Laurent Beauregard, Yuri Shimane"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.11043v1"
tags: ["query:cislunar-dyn-nav"]
score: 8.0
evidence: 航天器轨迹优化与打靶法构建
tldr: 本研究探讨了航天器轨道优化中零阶保持（ZOH）转录的实际应用，提出了一套无需特定调优即可在多种动力学环境下保持鲁棒性的设计原则。主要贡献包括：研究了前向-后向射击结构（ZOH_α）；引入冗余四维节流参数化以消除弹道弧段的奇异性；采用Softmax时间网格编码以确保全可微性；并发布了包含28个问题的TOPS基准测试集，涵盖二体、三体及太阳帆等多种模型。
selection_source: fresh_fetch
motivation: 旨在解决航天器轨道优化中ZOH转录方法的鲁棒性不足、参数化奇异性以及时间网格排序约束等实际实施难题。
method: 提出了前向-后向射击结构、冗余四维节流参数化方案以及基于Softmax的时间网格编码方法。
result: 通过包含28个跨动力学模型问题的TOPS基准测试集，验证了所提设计原则在不同场景下的有效性。
conclusion: 该研究为实现高效、稳健且无需特定调优的行星际轨道优化提供了一套实用的设计准则和标准化评估工具。
---

## 摘要
我们研究了用于航天器轨道优化的零阶保持器（ZOH）转录法的实际实现，并确定了一套设计原则，使其在广泛的动力学设置中具有鲁棒性，且无需针对特定问题进行调优。本文的贡献包括四个方面：(i) 对表示为 $\mathrm{ZOH}_α$ 的前向-后向打靶构造进行了深入研究；(ii) 提出了一种冗余的四维节流阀参数化方法，消除了沿弹道弧段控制影响矩阵的奇异性；(iii) 提出了一种 softmax 时间网格编码，在保持完全可微性的同时，避免了对路段持续时间的排序约束；(iv) 推出了 TOPS 基准测试（航天轨道优化问题），这是一套包含 28 个问题的测试集，涵盖了二体笛卡尔坐标、修正春分点根数、圆型限制性三体问题以及太阳帆四种动力学模型，并旨在随时间推移进行扩展。

## Abstract
We study the practical implementation of zero-order-hold (ZOH) transcriptions for spacecraft trajectory optimisation, identifying a set of design principles that render them robust across a broad class of dynamical settings without problem-specific tuning. The contributions are fourfold: (i) a thorough study of the forward--backward shooting construction, denoted $\mathrm{ZOH}_α$; (ii) a redundant four-dimensional throttle parameterization that eliminates the singularity of the control influence matrix along ballistic arcs; (iii) a softmax time-grid encoding that avoids ordering constraints on segment durations while preserving full differentiability; and (iv) the TOPS benchmark (Trajectory Optimisation Problems in Space), a suite of 28 problems spanning four dynamical models, two-body Cartesian, modified equinoctial elements, circular restricted three-body, and solar sailing, designed to be extended over time.

---

## 论文详细总结（自动生成）

这篇论文为航天器行星际轨道优化中的**零阶保持（Zero-Order Hold, ZOH）转录方法**提供了一套标准化的实施指南。作者旨在解决该方法在实际应用中对专家经验依赖重、数值奇异性以及调优复杂等痛点。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在低推力轨道优化中，ZOH 转录法（将控制输入简化为分段常数）因其符合燃料最优控制的“砰-砰”（Bang-Bang）特性而广受欢迎。然而，现有的实施方案往往需要针对特定问题进行大量手动调优，且在处理弹道弧段（无推力段）时存在数值奇异性，在处理变时间步长时存在排序约束难题。
*   **研究动机**：建立一套鲁棒、通用且无需特定调优的设计原则，使 ZOH 方法从一种“依赖专家直觉的艺术”转变为一种“系统化的计算方法”。

### 2. 方法论：核心思想与关键技术
论文提出了三个关键的技术改进：
*   **$\mathrm{ZOH}_\alpha$ 前向-后向打靶法**：
    *   不同于传统的单向打靶或多点打靶，该方法将轨道分为前向（从起点出发）和后向（从终点出发）两段，在中间点 $\alpha$ 处强制状态连续。
    *   这种结构降低了梯度敏感度，并能同时利用始末两端的边界条件，提高随机初始猜测的收敛率。
*   **冗余四维节流阀参数化（Redundant 4D Throttle）**：
    *   为了消除在推力为零时控制影响矩阵的奇异性，将控制向量从 3 维扩展为 4 维：一个标量节流阀大小 $\tau \in [0,1]$ 和一个 3 维方向向量 $d$（限制 $\|d\| \le 1$）。
    *   这种设计确保了即使在弹道弧段，导数信息依然平滑且可定义。
*   **Softmax 时间网格编码**：
    *   利用 Softmax 函数将无约束向量映射为各段时长，自动满足时长大于零且总和为 1 的约束。
    *   这消除了优化过程中对时间节点排序的硬性不等式约束，保持了全流程的可微性。

### 3. 实验设计
*   **基准测试集（TOPS）**：作者推出了“航天轨道优化问题”（TOPS）基准集，包含 28 个典型问题。
*   **动力学模型**：涵盖了二体笛卡尔坐标、修正春分点根数（MEE）、圆型限制性三体问题（CR3BP）以及太阳帆（Solar Sailing）动力学。
*   **对比维度**：
    *   **算法对比**：对比了 IPOPT（内点法）和 SNOPT（序列二次规划法）两种主流求解器。
    *   **参数消融**：对比了不同段数 $N$、不同切分点 $\alpha$、均匀 vs 变时间网格、以及节流阀约束的等式 vs 不等式形式。
*   **初始猜测**：每个配置运行 128 次独立的随机初始猜测，以评估算法的鲁棒性和全局搜索能力。

### 4. 资源与算力
*   **算力说明**：文中未明确提及具体的 GPU 或 CPU 型号及总训练时长。
*   **评估指标**：引入了**期望运行时间（ERT）**指标，这是一种硬件归一化的成本衡量方式，结合了目标函数评估次数和梯度评估次数，确保了实验结果在不同平台间的可移植性。

### 5. 实验数量与充分性
*   **实验规模**：总计进行了超过 **300,000 次** 求解器运行。
*   **充分性评价**：实验设计非常充分且客观。通过大规模随机多起点测试，排除了“特定初始猜测”带来的偏差；涵盖了从简单二体到复杂三体及太阳帆的多种动力学，验证了方法的通用性。

### 6. 主要结论与发现
*   **$\alpha=0.5$ 最优**：前向-后向结合的打靶结构在所有问题中均优于纯前向或纯后向打靶。
*   **求解器表现**：在经过正确缩放和全微分处理后，开源求解器 IPOPT 的表现与商业求解器 SNOPT 相当，甚至在某些冷启动场景下更具鲁棒性。
*   **Softmax 优势**：在太阳帆问题中，Softmax 时间网格编码表现出显著优势，因为它允许优化器灵活收缩不利的控制段。
*   **约束形式**：对于节流阀，使用不等式约束通常比等式约束更高效，除非轨道中包含大量极短的脉冲式弧段。

### 7. 优点
*   **实用性极强**：直接针对工程实现中的奇异性和收敛性问题给出了具体方案。
*   **全微分设计**：包括对行星历表的代理梯度处理，确保了整个优化链条的导数连续性。
*   **开源贡献**：发布了 TOPS 基准测试集和配套代码，极大地促进了该领域的透明度和可重复性。

### 8. 不足与局限
*   **模型复杂度限制**：虽然涵盖了四种动力学，但尚未涉及包含复杂摄动（如非球形引力、多体遮挡）的高保真模型。
*   **缩放依赖**：尽管提高了鲁棒性，但求解器性能依然高度依赖于问题的非量纲化缩放（Scaling），这仍需要一定的先验知识。
*   **局部最优风险**：本质上仍是基于梯度的局部优化框架，对于极度非凸的问题，仍需配合全局搜索策略。

（完）
