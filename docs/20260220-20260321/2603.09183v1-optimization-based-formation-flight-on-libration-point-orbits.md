---
title: Optimization-Based Formation Flight on Libration Point Orbits
title_zh: 基于优化的平动点轨道编队飞行
authors: "Yuri Shimane, Purnanand Elango, Avishai Weiss"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09183v1"
tags: ["query:cislunar-dyn-nav"]
score: 10.0
evidence: 针对平动点轨道航天器编队飞行的模型预测控制站位保持
tldr: 本研究针对平动点轨道上的航天器编队飞行，提出了一种基于模型预测控制（MPC）的站位保持框架。该方法通过求解多航天器最优控制问题，在满足有限轨道机动机会等操作约束的同时，利用序列凸规划解决非凸优化。通过路径约束紧致化和等周重构技术，确保了控制的递归可行性并防止采样间约束违规。在近直线晕轨道（NRHO）的高保真仿真验证中，该方案成功维持了编队构型，且燃料消耗与现有方法相当。
selection_source: fresh_fetch
motivation: 旨在解决平动点轨道编队飞行中，受限机动机会下的高精度站位保持与复杂路径约束避让问题。
method: 采用模型预测控制结合序列凸规划，并引入约束紧致化与等周重构技术以提升算法的鲁棒性与可行性。
result: 在高保真历表模型下的NRHO编队仿真证明，该方法能严苛执行航天器间距及太阳相位角约束。
conclusion: 该基于优化的控制策略在保证编队安全与任务约束的前提下，实现了与传统方法相当的燃料效率。
---

## 摘要
本文开发了一种模型预测控制（MPC）框架，用于沿平动点轨道运行的航天器编队飞行的轨道保持。在每个控制周期，MPC 策略求解一个多飞行器最优控制问题（MVOCP），在跟踪参考轨迹的同时，对编队的相对运动施加路径约束。该控制策略利用了一组有限的控制节点，以符合每圈仅允许少量机动机会的操作约束。为了提高递归可行性，路径约束在预测时域内被逐步收紧。采用约束的等周重构（isoperimetric reformulation）来防止采样点之间的违规。由此产生的 MVOCP 是一个非凸规划问题，通过序列凸规划进行求解。所提方法在具有现实不确定性的高保真历表模型中进行了评估，针对沿近直线晕轨道（NRHO）运行的编队，并考虑了航天器间距和相对太阳相位角的路径约束。结果表明，该方法能够维持满足路径约束的航天器编队，且累计推进剂消耗与现有方法相当。

## Abstract
A model predictive control (MPC) framework is developed for station-keeping in spacecraft formation flight along libration point orbits. At each control period, the MPC policy solves a multi-vehicle optimal control problem (MVOCP) that tracks a reference trajectory, while enforcing path constraints on the relative motion of the formation. The control policy makes use of a limited set of control nodes consistent with operational constraints that allow only a small number of maneuver opportunities per revolution. To promote recursive feasibility, path constraints are progressively tightened across the prediction horizon. An isoperimetric reformulation of the constraints is used to prevent inter-sample violations. The resulting MVOCP is a nonconvex program, which is solved via sequential convex programming. The proposed approach is evaluated in a high-fidelity ephemeris model under realistic uncertainties for a formation along the near-rectilinear halo orbit (NRHO), and subject to path constraints on interspacecraft separation and relative Sun phase angle. The results demonstrate maintenance of a spacecraft formation that satisfies the path constraints with cumulative propellant consumption comparable to that of existing methods

---

## 论文详细总结（自动生成）

### 基于优化的平动点轨道编队飞行论文总结

#### 1. 核心问题与研究背景
*   **研究动机**：随着月球门户站（Gateway）和地月空间星座任务的兴起，如何在复杂的平动点轨道（LPO）上实现多航天器安全、高效的编队飞行成为关键。
*   **核心问题**：现有的编队控制方法（如基于相对轨道根数或局部环面坐标的方法）往往将绝对站位保持与相对构型维持解耦，且多依赖简化动力学模型（如CR3BP），难以在高保真历表模型（HFEM）及存在多种不确定性（导航误差、机动执行误差等）的环境下提供严苛的安全保证和燃料最优性。
*   **整体含义**：本文旨在开发一个统一的、基于优化的模型预测控制（MPC）框架，在考虑现实操作约束（如每圈仅两次机动）的前提下，同时处理绝对轨道跟踪和复杂的相对运动路径约束。

#### 2. 方法论
*   **核心思想**：将编队飞行建模为一个集中式的多航天器最优控制问题（MVOCP），通过最小化全编队的总推进剂消耗（$\Delta v$），在预测时域内满足动力学和任务约束。
*   **关键技术细节**：
    *   **多航天器最优控制（MVOCP）**：目标函数为所有航天器脉冲机动幅值的平方和。约束包括高保真动力学方程、初始状态估计、终端参考轨道跟踪误差以及相对运动路径约束。
    *   **路径约束紧致化（Constraint Tightening）**：为了应对不确定性并确保MPC的递归可行性，引入启发式紧致函数，随预测时域增加逐步收紧约束边界，为未来的扰动留出余量。
    *   **等周重构（Isoperimetric Reformulation）**：为了防止在离散控制节点之间发生约束违规（采样间违规），将连续时间路径约束转化为等周积分形式，并引入松弛状态变量，确保在整个时间段内满足安全要求。
    *   **序列凸规划（SCP）**：利用基于增广拉格朗日的SCP算法解决非凸的MVOCP问题，通过对动力学方程进行线性化并迭代求解凸子问题来逼近原问题的局部最优解。

#### 3. 实验设计
*   **场景设定**：以地月系统中的 9:2 共振近直线晕轨道（NRHO）为参考轨道，模拟两个航天器的编队飞行。
*   **高保真模型**：包含月球 4x4 阶球谐函数引力、地球和太阳的第三体引力、太阳光压（SRP）以及历表模型。
*   **对比实验（Benchmark）**：
    *   **实验 I & II**：仅考虑相对距离约束（最小/最大距离），对比“连续时间约束（等周法）”与“离散节点约束”。
    *   **实验 III & IV**：同时考虑相对距离和太阳相位角约束（确保光学可见性），对比连续与离散约束处理方式。
    *   **灵敏度分析**：针对约束紧致参数（$\eta$）进行消融实验，验证其对递归可行性的影响。

#### 4. 资源与算力
*   **软件环境**：使用 Julia 语言编写，利用 `DifferentialEquations.jl` 进行数值积分，`JuMP` 进行建模，`Clarabel` 作为底层凸优化求解器。
*   **算力说明**：文中未明确提及具体的硬件型号（如 CPU/GPU 核心数），但指出 SCP 算法中最耗时的部分是状态转移矩阵（STM）的积分，该过程可以通过并行化加速。

#### 5. 实验数量与充分性
*   **实验规模**：进行了 4 组主要的蒙特卡洛（Monte Carlo）实验，每组包含 100 个随机样本，每个样本模拟时长为 10 个轨道周期（约 65.5 天）。
*   **充分性评价**：实验涵盖了初始入轨误差、动力学建模误差（SRP 参数波动）、导航误差和控制执行误差（Gates 模型）。通过数百次随机仿真验证了算法在统计意义上的鲁棒性和燃料消耗分布，实验设计较为客观、全面。

#### 6. 主要结论与发现
*   **安全性**：采用等周重构的连续时间约束处理方法能有效消除采样点间的约束违规，而仅在控制节点施加约束的方法会导致频繁的安全违规。
*   **燃料效率**：所提方法的年均站位保持成本约为每台航天器 4.2 m/s，与现有的解耦控制方法相当，但在约束处理上更严苛。
*   **约束代价**：引入额外的太阳相位角约束对燃料成本的影响较小（3-$\sigma$ 成本仅增加约 2.4%），证明了该优化框架在处理多重复杂约束时的灵活性。
*   **递归可行性**：适当的约束紧致化是确保 MPC 在长期运行中不因随机扰动而陷入不可解状态的关键。

#### 7. 优点
*   **统一性**：将绝对站位保持与相对编队约束整合进单一优化框架，避免了模块化设计带来的次优性。
*   **操作现实性**：严格遵循每圈仅两次机动的低频控制节奏，符合月球门户站等真实任务的操作限制。
*   **严谨的安全机制**：通过等周重构解决了轨迹优化中常见的“采样间违规”难题。

#### 8. 不足与局限
*   **计算复杂度**：集中式 MPC 随着航天器数量 $M$ 的增加，决策变量和约束数量呈平方级增长，可能难以扩展到超大规模集群。
*   **参数依赖**：约束紧致化的参数（如 $\eta$ 和 $\kappa$）目前依赖启发式调节，缺乏自动化的最优参数确定方法。
*   **通信假设**：集中式方案假设航天器间或与地面站有完美的实时通信来同步状态信息，未考虑通信延迟或链路中断的影响。

（完）
