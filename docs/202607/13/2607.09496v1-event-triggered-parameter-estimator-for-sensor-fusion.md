---
title: Event-triggered parameter estimator for sensor fusion
title_zh: 传感器融合的事件触发参数估计器
authors: "Ariana Méndez-Castillo, Irene Perez-Salesa, Rodrigo Aldana-López, Antonio Ramírez-Treviño, Rosario Aragues"
date: 2026-07-10
pdf: "https://arxiv.org/pdf/2607.09496v1"
tags: ["query:cislunar-dyn-nav"]
score: 8.0
evidence: 用于传感器融合的事件触发参数估计器，节省通信
tldr: 针对传感器融合系统中的事件触发参数估计问题，提出一种仅依赖回归信号的局部触发规则，无需当前参数估计。在持续激励条件下，通过时变Lyapunov函数推导出估计器增益与事件阈值的显式设计不等式，保证全局指数收敛。进一步给出回归器动力学条件以排除Zeno行为。仿真验证了该方法在显著节省通信的同时保持指数收敛性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.09496v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 755, \"height\": 525, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09496v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 891, \"height\": 1235, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09496v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 893, \"height\": 661, \"label\": \"Figure\"}]"
motivation: 现有事件触发参数估计依赖当前参数估计，需在传感器端与估计器之间频繁交互，通信开销大。
method: 提出回归器驱动的局部触发规则，基于持续激励条件和时变Lyapunov分析，推导增益与阈值的显式不等式。
result: 全局指数收敛得以保证，仿真表明通信量显著减少且收敛速度未受影响。
conclusion: 该事件触发机制在保持参数估计精度的同时大幅降低通信负载，适用于资源受限的传感器网络。
---

## 摘要
本文研究了传感器融合系统中的事件触发参数估计，其中传感器将测量值传输给基于梯度的估计器。我们提出了一种由回归量驱动的局部触发规则，该规则无需知道当前参数估计值，仅依赖于回归量信号。在聚合回归量满足持续激励条件下，我们推导了估计器增益和事件阈值的显式设计不等式，保证了全局指数收敛。分析基于时变李雅普诺夫函数。我们进一步给出了回归量动态的一个充分条件，该条件强制执行事件间时间的统一下界，排除了芝诺行为。仿真结果表明，在保持指数收敛的同时，通信量大幅减少。

## Abstract
This paper studies event-triggered parameter estimation in sensor fusion systems where sensors transmit measurements to a gradient based estimator. We introduce a regressor-driven local triggering rule that requires no knowledge of the current parameter estimate and depends solely on the regressor signals. Under a persistent excitation condition on the aggregate regressor, we derive explicit design inequalities on the estimator gain and event thresholds that guarantee global exponential convergence. The analysis is based on a time-varying Lyapunov function. We further provide a sufficient condition on the regressor dynamics that enforces a uniform lower bound on inter-event times, excluding Zeno behavior. Simulations show substantial communication savings while preserving exponential convergence.

---

## 论文详细总结（自动生成）

# 传感器融合的事件触发参数估计器——论文详细总结

## 1. 核心问题与整体含义（研究动机与背景）
- **问题**：在传感器网络中，多个传感器收集测量数据，期望对未知参数向量θ进行全局估计。但传统参数估计方法（如连续时间Kalman-Bucy滤波器、梯度估计器）要求传感器与估计器之间持续通信，这在通信资源受限的场景下（如无线传感器网络、无人水面艇）代价高昂。
- **动机**：现有事件触发参数估计方法通常需要每个传感器实时获取当前参数估计值（例如基于预测误差的触发规则），这引入了额外的计算与通信开销，且大多仅能实现渐近收敛而非指数收敛。本文旨在设计一种**仅依赖本地回归信号**的事件触发规则，无需参数估计值，同时保证**全局指数收敛**。
- **背景**：持续激励（PE）条件是实现梯度估计器指数收敛的经典假设。事件触发机制（ETM）可有效减少通信，但需避免Zeno行为（事件无限密集）。本文结合PE条件、时变Lyapunov函数和事件触发，为传感器融合提供一种通信节约且收敛性强的参数估计方案。

## 2. 方法论：核心思想、关键技术细节
- **核心思想**：每个传感器i定义本地触发规则（公式1a），仅当回归量二次型的变化超过阈值Δᵢ时，才向估计器发送当前测量值yᵢ(t)和回归矩阵Cᵢ(t)。估计器（公式1c）运行连续时间梯度更新，但使用**分段常数**的聚合数据（最近一次收到的所有传感器数据）。
- **关键技术细节**：
  - **局部触发规则**：τᵢₖ₊₁ = sup{ t ≥ τᵢₖ : ‖Cᵢ(τᵢₖ)ᵀCᵢ(τᵢₖ) - Cᵢ(t)ᵀCᵢ(t)‖ ≥ Δᵢ }。完全依赖于本地的回归信号，无需参数估计值。
  - **估计器模型**：˙θ̂(t) = -γ C̃(t)ᵀ (C̃(t) θ̂(t) - ỹ(t))，其中C̃, ỹ是聚合的时不变分段数据。
  - **误差动力学重写**：Lemma 4 将误差动力学化为标称项（-γCᵀC θ̃）加扰动项（γ Δ(t) θ̃），其中Δ(t)= CᵀC - C̃ᵀC̃。
  - **持续激励假设**（Assumption 2）：聚合回归矩阵C(t)在滑动窗口T内满足m Iₙ ≤ ∫_{t-T}^{t} CᵀC dτ，且‖C(t)‖ ≤ b。
  - **Lyapunov函数设计**：采用时变二次型V = θ̃ᵀP(t) θ̃，其中P(t)包含积分项（公式10），在PE条件下满足k₁‖θ̃‖² ≤ V ≤ k₂‖θ̃‖²。
  - **收敛条件**：Theorem 3 给出充分条件：γ > 0，Δᵢ ≤ m/(4γk₂N)。在此条件下，导数V̇ ≤ -α V，保证全局指数收敛。
  - **Zeno行为排除**：Proposition 1 给出充分条件：若‖Cᵢ(t)‖ ≤ bᵢ，‖Ċᵢ(t)‖ ≤ cᵢ，则事件间隔存在严格正下界τ > 0（基于均值定理的反证法）。

## 3. 实验设计
- **数据/场景**：使用数值仿真，包含N=3个传感器，未知参数向量θ∈ℝ⁵（从[-10,10]均匀采样）。每个传感器的回归矩阵Cᵢ(t)∈ℝ²ˣ⁵，其元素由A + B sin(ωt) + C cos(ωt)组成，参数A,B,C,ω从[0,3]均匀采样。仿真时长未明确。
- **Benchmark**：没有显式对比其他事件触发方法（如send on delta、随机触发、动态触发等）。仅与无事件触发的理想情况（连续通信）进行隐含对比（通过分析通信节省量）。
- **对比方法**：论文内部比较了不同阈值Δ（从0到20）下的通信次数和收敛时间，但未与其他文献中的事件触发参数估计方法进行定量比较。
- **评估指标**：估计误差范数‖θ̂ - θ‖，收敛时间（定义为‖θ̂ - θ‖ ≤ 0.5的最小时刻），总事件触发次数。

## 4. 资源与算力
- **未明确说明**：论文是纯理论+数值仿真，没有提及GPU型号、数量、训练时长等。所有实验应在普通CPU上运行，计算量很小。因此无法总结算力资源。

## 5. 实验数量与充分性
- **实验数量**：仅提供了一个示例场景（N=3, n=5, pi=2）。变化参数为阈值Δ，观察了Δ从0到20时的通信次数和收敛时间（图3）。另外展示了Δ=2.8时的估计误差、估计值和触发事件的时间序列（图2）。
- **充分性**：
  - 优点：验证了核心理论（指数收敛）和通信节省效果，展示了收敛时间与阈值之间的权衡。
  - **不足**：① 仅单次随机种子，没有多次重复实验（如不同参数向量、不同回归信号相位）的平均性能。② 没有消融实验（如改变传感器数量N、维度n、增益γ等）。③ 缺少与现有事件触发参数估计方法的对比（如基于预测误差的触发、send on delta）。④ 理论条件（Δ ≤ 3.2×10⁻⁴）非常保守，而实验中Δ可取到20，论文本身指出了理论保守性，但未给出一种非保守的松弛分析。因此，实验验证了可行性，但**不够系统全面**。

## 6. 主要结论与发现
- 提出一种仅依赖回归信号**无需参数估计**的局部事件触发规则，适用于传感器融合梯度估计。
- 在聚合回归量满足PE条件下，通过时变Lyapunov分析得到增益γ和阈值Δᵢ的显式不等式，保证**全局指数收敛**。
- 给出了回归器动力学条件（有界性及导数有界性）确保**最小事件间隔存在**（排除Zeno）。
- 数值仿真表明，与连续通信相比，事件触发可大幅减少传感器传输次数（例如Δ=2.8时通信显著减少），同时保持估计误差指数收敛。尽管理论阈值非常保守，实际中更大阈值仍能收敛，说明理论条件可进一步放松。

## 7. 优点
- **创新触发规则**：传感器仅需本地回归信号，无需访问当前参数估计，降低了传感器计算和通信要求，适用于资源受限场景。
- **强收敛保证**：实现了全局指数收敛（而非渐近收敛），且在PE条件下通过Lyapunov给出了显式增益/阈值不等式，便于设计。
- **Zeno避免**：给出回归器动态的充分条件，保障最小事件间隔，具有工程实用性。
- **简化通信架构**：估计器只需处理分段常数数据，无需处理异步触发。
- **直接理论证明**：从误差动力学到Lyapunov不等式推导完整，贡献清晰。

## 8. 不足与局限
- **理论条件保守**：文中设计条件Δᵢ ≤ m/(4γk₂N) 导致阈值极小（示例中约10⁻⁴数量级），而实验中Δ可取到20仍收敛，说明条件远非必要，不利于实际应用。论文虽指出这一点，但未提供更松弛的充分条件。
- **实验验证单薄**：仅一个示例（3传感器，5维参数），缺少多场景、多次随机实验统计，也未与基准事件触发方法（如send on delta、基于预测误差触发）对比。
- **未考虑噪声**：假设测量无噪声（y=Cθ），未考虑实际传感器噪声对触发和收敛的影响。
- **假设依赖性强**：PE条件是全局指数收敛的关键，但验证PE条件在实际系统中不一定容易。
- **网络拓扑假设**：假设所有传感器直接发送给中央估计器（星型），未扩展到分布式估计（如无中心节点）。传感器之间无通信交互。
- **实时性分析缺失**：未分析事件触发带来的延迟、丢包等问题，也未考虑估计器接收数据顺序的影响。

（完）
