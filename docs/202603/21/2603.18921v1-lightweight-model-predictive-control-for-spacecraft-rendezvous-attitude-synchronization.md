---
title: Lightweight Model Predictive Control for Spacecraft Rendezvous Attitude Synchronization
title_zh: 航天器交会姿态同步的轻量级模型预测控制
authors: "Peter Stadler, Alexander Meinert, Niklas Baldauf, Alen Turnwald"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18921v1"
tags: ["query:cislunar-dyn-nav"]
score: 8.0
evidence: 航天器交会对接姿态同步与模型预测控制
tldr: 针对航天器交会对接中的姿态同步问题，本文提出了两种轻量级模型预测控制（MPC）方法。通过引入新型姿态偏差公式，实现了角速度的线性约束。研究开发了单环和双环MPC架构，并利用CasADi进行自动代码生成。实验表明，该方法在提高跟踪精度的同时，显著降低了计算开销和内存占用，并在ARM Cortex-M7嵌入式平台上验证了其实时性，非常适合资源受限的商业航天任务。
selection_source: fresh_fetch
motivation: 旨在解决资源受限的航天器在交会对接过程中，姿态同步控制对计算效率和精度的双重需求。
method: 提出基于新型姿态偏差公式的单环和双环轻量级MPC，其中双环结构通过内嵌稳定反馈控制器简化为线性定常系统。
result: 实验验证表明，该方法在提升跟踪精度的同时，有效减少了计算功耗和内存消耗。
conclusion: 该轻量级MPC方案在嵌入式硬件上表现出良好的实时性，是资源受限航天器机载姿态控制的理想选择。
---

## 摘要
本研究针对航天器交会同步过程中利用反作用轮进行姿态跟踪的问题，提出了两种轻量级模型预测控制（MPC）方法。这两种方法均基于一种新型姿态偏差公式，该公式允许对角速度使用固有的线性约束。我们开发了单环和双环 MPC；后者在内环中嵌入了稳定反馈控制器，从而构建了一个线性时不变系统。两种控制器均采用 CasADi 实现（包括自动代码生成），在多种求解器上进行了评估，并在 Basilisk 天体动力学仿真框架中进行了验证。实验结果表明，该方法在提高跟踪精度的同时，降低了计算开销和内存消耗。最后，在 ARM Cortex-M7（代表“新航天”平台中使用的商用现货设备）上的嵌入式部署证实了这些方法的实时可行性，并突显了其在资源受限的航天器交会任务中进行星载姿态控制的适用性。

## Abstract
This work introduces two lightweight model predictive control (MPC) approaches for attitude tracking with reaction wheels during spacecraft rendezvous synchronization. Both approaches are based on a novel attitude deviation formulation, which enables the use of inherently linear constraints on angular velocity. We develop a single-loop and a dual-loop MPC; the latter embeds a stabilizing feedback controller within the inner loop, yielding a linear time-invariant system. Both controllers are implemented with CasADi - including automatic code generation - evaluated across various solvers, and validated within the Basilisk astrodynamics simulation framework. The experimental results demonstrate improved tracking accuracy alongside reductions in computational effort and memory consumption. Finally, embedded delivery to an ARM Cortex-M7 - representative of commercial off-the-shelf devices used in New Space platforms - confirms the real-time feasibility of these approaches and highlights their suitability for onboard attitude control in resource-constrained spacecraft rendezvous missions.

---

## 论文详细总结（自动生成）

### 航天器交会姿态同步的轻量级模型预测控制论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：在航天器交会对接（Rendezvous）任务中，服务航天器需要与目标航天器实现高精度的姿态同步（包括旋转角度和角速度）。
*   **研究动机**：
    *   **资源受限**：传统的非线性模型预测控制（NMPC）虽然精度高且能处理约束，但计算开销巨大，难以在资源受限的微小卫星机载计算机上运行。
    *   **线性化挑战**：标准线性 MPC 在处理姿态旋转时，由于旋转矩阵或四元数的非线性特性，难以在保持计算效率的同时精确处理角速度约束。
    *   **新航天需求**：商业航天（New Space）倾向于使用商用现货（COTS）硬件，迫切需要能在低功耗嵌入式处理器上实时运行的轻量级控制算法。

#### 2. 方法论
论文提出了两种基于新型姿态偏差定义的线性 MPC 方案：
*   **新型姿态偏差定义**：采用反向乘法顺序定义姿态误差 $\Delta R = R R_d^T$（其中 $R$ 为当前姿态，$R_d$ 为期望姿态）。这种定义的关键优势在于，它使得预测模型中的角速度约束变为**固有线性**，避免了传统定义在预测过程中因非线性导致的精度损失。
*   **单环 MPC（Single-Loop）**：
    *   直接对基于旋转向量的误差系统进行线性化。
    *   得到一个线性参数变化（LPV）系统，模型随参考轨迹动态更新。
*   **双环 MPC（Dual-Loop）**：
    *   **内环**：嵌入一个基于 Lyapunov 稳定性的反馈控制器，用于预先稳定系统。
    *   **外环**：MPC 作用于闭环后的内环系统。由于内环已稳定，外环看到的模型简化为线性时不变（LTI）系统。
*   **技术实现**：利用 **CasADi** 框架进行符号建模和自动 C 代码生成，支持多种二次规划（QP）求解器（如 OSQP, DAQP, qpOASES 等）。

#### 3. 实验设计
*   **仿真环境**：使用高保真天体动力学仿真框架 **Basilisk**。
*   **对比基准（Benchmark）**：对比了 Turnwald 等人于 2023 年提出的基于四元数差值的双环 MPC 方法。
*   **测试场景**：模拟航天器在交会阶段的姿态跟踪任务，设定了严格的反作用轮转矩约束和角速度限制。
*   **评估指标**：跟踪误差（角度与角速度）、计算耗时（Runtime）、内存占用（RAM/Flash）、累积控制功耗（Torque effort）。

#### 4. 资源与算力
*   **桌面端测试**：Intel i7-11850H 处理器，用于评估不同 QP 求解器在不同预测时域（Horizon）下的性能。
*   **嵌入式端验证**：**ARM Cortex-M7 (Teensy 4.1)** 处理器。这是典型的 COTS 硬件，代表了微小卫星的算力水平。
*   **训练时长**：由于该方法基于经典控制理论和在线优化，不涉及深度学习训练过程。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 25 秒的高保真闭环仿真对比。
    *   对 9 种不同的开源 QP 求解器进行了横向对比。
    *   测试了预测时域从 5 步到 50 步不等的性能变化。
*   **充分性评价**：实验设计较为充分。不仅在 PC 端验证了算法逻辑，还深入探讨了不同稀疏矩阵模式对计算效率的影响，并最终在嵌入式硬件上完成了实时性验证，客观地展示了算法在实际工程中的可行性。

#### 6. 主要结论与发现
*   **性能提升**：提出的两种 MPC 方法在跟踪精度上均优于基准方法，且误差收敛速度更快。
*   **双环优势**：双环 MPC 在计算效率和内存占用上表现最优。由于其基于 LTI 系统，矩阵预处理更简单，适合极低资源环境。
*   **求解器表现**：在嵌入式平台上，**DAQP** 求解器在短时域（<20步）下速度最快；而 **OSQP** 在长时域下由于其稀疏矩阵处理能力，表现更稳健且内存增长线性。
*   **实时可行性**：在 ARM Cortex-M7 上，双环 MPC 可以在 30 步预测时域内实现实时控制，满足航天器控制频率要求。

#### 7. 优点
*   **数学巧妙性**：通过反转姿态误差定义，巧妙地解决了角速度约束的线性化问题。
*   **工程实用性**：提供了从理论推导到代码生成再到嵌入式部署的完整链路，对实际卫星工程有直接指导意义。
*   **轻量化**：双环 LTI 结构极大地降低了机载计算负担。

#### 8. 不足与局限
*   **自由度限制**：目前仅讨论了 3 自由度的姿态控制，未涉及平移运动（即未实现完整的 6 自由度相对动力学控制）。
*   **鲁棒性探讨较少**：虽然提到了未来可扩展为鲁棒 MPC，但本论文实验中未深入测试在大幅度参数扰动（如惯量矩阵大幅变化）下的表现。
*   **内存瓶颈**：在嵌入式平台上，内存（RAM）依然是限制预测时域增加的主要因素，对于更复杂的任务可能需要进一步优化内存管理。

（完）
