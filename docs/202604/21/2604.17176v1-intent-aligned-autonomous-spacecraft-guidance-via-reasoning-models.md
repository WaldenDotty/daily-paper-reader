---
title: Intent-aligned Autonomous Spacecraft Guidance via Reasoning Models
title_zh: 基于推理模型的意图对齐自主航天器制导
authors: "Yuji Takubo, Simone D'Amico"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17176v1"
tags: ["query:cislunar-dyn-nav"]
score: 8.0
evidence: 自主航天器制导与轨迹优化
tldr: "针对未来航天器自主运行中难以理解高层任务意图且过度依赖专家建模的问题，本文提出一种意图对齐的自主引导框架。该框架通过行为序列和航路点约束作为中间抽象，将大模型的高层推理与安全轨迹优化相结合。实验证明，该方法在近距离操作场景中实现了超过90%的收敛率，且意图满足率比启发式方法高出1.5倍，为安全关键的航天器自主性提供了实用接口。"
selection_source: fresh_fetch
motivation: 现有的航天器轨迹优化高度依赖专家手工建模，缺乏对高层任务意图的理解和条件决策能力。
method: 提出一种分解式框架，利用基础模型预测行为计划并转化为航路点约束，随后通过优化算法计算安全轨迹。
result: "在近距离操作实验中，该方法实现了超过90%的算法收敛率，且在意图优先的性能指标上比启发式决策高出1.5倍。"
conclusion: 引入中间行为抽象层是连接基础模型推理与安全关键型航天器自主引导的有效且实用的途径。
---

## 摘要
未来的航天器操作需要能够在保证安全的同时解释高级任务意图的自主系统。然而，现有的轨迹优化仍严重依赖专家设计的公式，且不支持基于意图条件的决策。本文提出了一种意图对齐的航天器制导框架，该框架通过基于行为序列和航路点约束的显式中间抽象，将高级推理与安全轨迹优化联系起来。首先，基础模型预测意图对齐的行为计划，随后航路点生成模型将其转换为航路点约束，最后通过优化计算出安全轨迹。这种分解在不牺牲安全性的前提下实现了可扩展的监督。在近距离操作场景中的数值实验表明，所提出的流程实现了超过 90% 的 SCP 收敛，并且与启发式决策相比，生成满足最高意图优先级性能标准的轨迹的比例高出 1.5 倍。这些结果支持将中间行为抽象作为基础模型推理与安全关键型星载航天器自主系统之间的实用接口。

## Abstract
Future spacecraft operations require autonomy that can interpret high-level mission intent while preserving safety. However, existing trajectory optimization still relies heavily on expert-crafted formulations and does not support intent-conditioned decision-making. This paper proposes an intent-aligned spacecraft guidance framework that links high-level reasoning and safe trajectory optimization through explicit intermediate abstractions, based on behavior sequences and waypoint constraints. A foundation model first predicts an intent-aligned behavior plan, a waypoint generation model then converts it into waypoint constraints, and the safe trajectory is computed via optimization. This decomposition enables scalable supervision without sacrificing safety. Numerical experiments in close-proximity operation scenarios demonstrate that the proposed pipeline achieves over 90\% SCP convergence and yields a $1.5\times$ higher rate of generating trajectories that satisfy the top intent-prioritized performance criteria than heuristic decision-making. These results support the use of intermediate behavior abstraction as a practical interface between foundation-model reasoning and safety-critical onboard spacecraft autonomy.

---

## 论文详细总结（自动生成）

这是一份关于论文《Intent-aligned Autonomous Spacecraft Guidance via Reasoning Models》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何让航天器自主系统既能理解人类的高层任务意图（如“优先省油”或“优先观测”），又能保证在复杂的近距离操作中满足严苛的物理动力学约束和碰撞规避安全性。
*   **研究动机**：
    *   **专家依赖性**：现有的轨迹优化方法（如连续凸规划 SCP）需要专家手工设计复杂的代价函数和约束，难以直接处理模糊的任务意图。
    *   **启发式局限**：目前的自主引导多依赖预设的航路点（Waypoint）逻辑，缺乏灵活性。
    *   **基础模型（FM）的局限**：虽然大模型具备推理能力，但直接输出控制指令（End-to-End）无法保证航天器操作的绝对安全性和动态可行性。

### 2. 方法论
论文提出了一种**三层分级决策架构**，通过“中间抽象层”连接高层推理与底层控制：
1.  **推理模型（Reasoning Model）**：
    *   基于微调的大语言模型（Qwen2.5-7B），输入任务上下文和意图优先级（如：燃料 > 时间 > 观测）。
    *   输出：行为序列（Behavior Sequence，如“漂移→接近→保持”）和推理链条（Reasoning Trace）。
2.  **航路点生成模型（Waypoint Generator）**：
    *   使用多层感知机（MLP），将抽象的行为序列转化为具体的几何航路点约束（位置和时间间隔）。
    *   采用**加权模仿学习**进行训练，权重由底层优化结果的奖励函数决定。
3.  **安全轨迹优化（SCP Solver）**：
    *   接收航路点作为硬约束，利用连续凸规划（SCP）在准无奇异相对轨道要素（qnsROE）空间内求解。
    *   **关键技术**：引入了“机会约束被动安全性”（Chance-constrained passive safety），确保即使在控制失效的情况下，航天器也能依靠自然漂移避开碰撞区（KOZ）。

### 3. 实验设计
*   **实验场景**：设定了三种典型的近距离操作任务：**绕飞（Circumnavigation）**、**飞越（Flyby）**和**下潜（Ducking）**。
*   **数据集**：生成了 50,000 组样本用于训练航路点生成器，并利用 LLM 自动标注了推理数据。
*   **对比基准（Benchmark）**：
    *   **启发式决策**：基于规则随机采样行为序列和航路点。
    *   **消融对比**：对比了“启发式推理+启发式航路点”、“启发式推理+神经网络航路点”、“神经网络推理+神经网络航路点”等四种组合。
*   **评估指标**：SCP 收敛成功率、奖励值（燃料/观测综合得分）、意图对齐一致性（语义匹配度）。

### 4. 资源与算力
*   **模型细节**：使用了 **Qwen2.5-7B-Instruct** 作为基础推理模型。
*   **训练技术**：采用了 **4-bit 量化**加载基础模型，并使用 **LoRA（低秩适配）**进行参数高效微调。
*   **算力说明**：文中**未明确列出**具体的 GPU 型号、数量及总训练时长，但提到了使用 LoRA 降低了内存和计算开销。

### 5. 实验数量与充分性
*   **实验规模**：在 500 组独立测试场景下进行了端到端的统计评估。
*   **充分性**：
    *   实验涵盖了从单模块验证（航路点生成精度）到全流程系统性能的测试。
    *   通过对比不同模块组合，清晰地展示了神经网络推理层对“意图对齐”的贡献。
    *   **客观性**：使用了确定性推理进行评估以确保可重复性，并引入了 GPT-4o-mini 作为第三方评估器来量化语义一致性，设计较为严谨。

### 6. 主要结论与发现
*   **高收敛性**：所提出的框架在底层 SCP 优化中实现了**超过 90% 的收敛成功率**，远高于启发式方法的 72.2%。
*   **意图对齐**：在满足高优先级任务意图方面，该框架比启发式决策表现高出 **1.5 倍**。
*   **语义一致性**：推理模型生成的行为序列 100% 可行，且超过 50% 的推理链条能完美匹配所有意图优先级。
*   **中间层价值**：证明了“行为序列”和“航路点”作为中间接口，能有效将模糊的语言意图转化为精确的物理约束。

### 7. 优点与亮点
*   **安全性保障**：没有让 LLM 直接控制航天器，而是将其置于优化器之上，通过物理约束求解器（SCP）作为“安全护栏”。
*   **可解释性**：推理模型输出的 Reasoning Trace 让操作员能理解系统为何选择特定路径，支持人工干预。
*   **引导式学习**：提出了一种分阶段引导策略，利用底层优化的结果反向监督高层推理模型的训练，解决了高层指令缺乏标注数据的问题。

### 8. 不足与局限
*   **行为空间受限**：目前的行为序列基于预定义的图结构（Waypoint-Behavior Graph），限制了系统处理完全未知任务的能力。
*   **泛化风险**：实验主要集中在近距离相对运动，尚未验证在深空探测或更复杂动力学环境下的泛化性能。
*   **实时性挑战**：虽然 SCP 求解很快，但 7B 规模的模型在星载计算资源受限的环境下可能存在推理延迟问题（文中未深入讨论星载部署的功耗约束）。

（完）
