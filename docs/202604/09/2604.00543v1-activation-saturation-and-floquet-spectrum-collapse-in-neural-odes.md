---
title: Activation Saturation and Floquet Spectrum Collapse in Neural ODEs
title_zh: 神经 ODE 中的激活饱和与 Floquet 谱坍缩
authors: Nikolaos M. Matzakos
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00543v1"
tags: ["query:cislunar-dyn-nav"]
score: 6.0
evidence: Floquet谱与周期轨道稳定性分析
tldr: 本研究揭示了神经常微分方程（Neural ODEs）中激活函数饱和对动力学特性的结构性限制。研究证明，当MLP层中的激活函数（如tanh或sigmoid）进入饱和区时，输入雅可比矩阵会受到衰减，导致周期轨道上的Floquet（李雅普诺夫）谱发生坍缩。随着饱和程度加深，所有指数均趋向于零，这从根本上限制了模型表现强收缩或混沌敏感性的能力。该理论为Neural ODEs在模拟复杂神经元动力学时的失效提供了数学解释。
selection_source: fresh_fetch
motivation: 探究激活函数饱和如何从结构上限制神经常微分方程捕捉复杂动力学行为（如强收缩或混沌）的能力。
method: 通过数学证明建立了MLP层饱和度与输入雅可比矩阵范数之间的关系，并利用Floquet理论分析了其对周期轨道特征指数的影响。
result: 证明了饱和会导致Floquet谱坍缩至零附近，并提出了一个改进的饱和加权谱分解界限，在多个振荡器模型上得到了数值验证。
conclusion: 激活函数饱和是Neural ODEs的一种结构性障碍，限制了其在推理阶段学习复杂向量场的能力，解释了其在特定生物物理模型上的失效。
---

## 摘要
我们证明了激活饱和对具有饱和激活函数（如 $\tanh$、sigmoid 等）的自治神经 ODE $\dot{h}=f_θ(h)$ 施加了结构性的动力学限制：如果在区域 $U$ 上，MLP $f_θ$ 的 $q$ 个隐藏层满足 $|σ'|\leδ$，则输入雅可比矩阵会发生衰减，满足 $\norm{Df_θ(x)}\le C(U)$（对于满足 $\sup_{x}|σ'(x)|\le 1$ 的激活函数，如 $\tanh$ 和 sigmoid，该界限简化为 $C_Wδ^q$），这迫使沿任何 $T$-周期轨道 $γ\subset U$ 的每个 Floquet（李雅普诺夫）指数都落入区间 $[-C(U),\;C(U)]$。这就是 Floquet 谱的坍缩：随着饱和程度加深（$δ\to 0$），所有指数都被推向零，从而限制了强收缩性和混沌敏感性。这种障碍是结构性的——它在推理阶段约束了学习到的向量场，且与训练质量无关。作为次要贡献，对于 $σ'>0$ 的激活函数，一种饱和加权的谱分解给出了一个更精细的界限 $\widetilde{C}(U)\le C(U)$，其改进在流（flow）层面上随 $T$ 呈指数级放大。所有结果均在 Stuart--Landau 振荡器上进行了数值演示；这些界限为 $\tanh$-NODEs 在 Morris--Lecar 神经元模型上经验性失效的现象提供了理论解释。

## Abstract
We prove that activation saturation imposes a structural dynamical limitation on autonomous Neural ODEs $\dot{h}=f_θ(h)$ with saturating activations ($\tanh$, sigmoid, etc.): if $q$ hidden layers of the MLP $f_θ$ satisfy $|σ'|\leδ$ on a region~$U$, the input Jacobian is attenuated as $\norm{Df_θ(x)}\le C(U)$ (for activations with $\sup_{x}|σ'(x)|\le 1$, e.g.\ $\tanh$ and sigmoid, this reduces to $C_Wδ^q$), forcing every Floquet (Lyapunov) exponen along any $T$-periodic orbit $γ\subset U$ into the interval $[-C(U),\;C(U)]$. This is a collapse of the Floquet spectrum: as saturation deepens ($δ\to 0$), all exponents are driven to zero, limiting both strong contraction and chaotic sensitivity. The obstruction is structural -- it constrains the learned vector field at inference time, independent of training quality. As a secondary contribution, for activations with $σ'>0$, a saturation-weighted spectral factorisation yields a refined bound $\widetilde{C}(U)\le C(U)$ whose improvement is amplified exponentially in~$T$ at the flow level. All results are numerically illustrated on the Stuart--Landau oscillator; the bounds provide a theoretical explanation for the empirically observed failure of $\tanh$-NODEs on the Morris--Lecar neuron model.