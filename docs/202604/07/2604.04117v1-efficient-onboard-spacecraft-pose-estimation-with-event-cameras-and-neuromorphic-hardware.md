---
title: Efficient Onboard Spacecraft Pose Estimation with Event Cameras and Neuromorphic Hardware
title_zh: 基于事件相机与类脑硬件的高效星载航天器位姿估计
authors: "Arunkumar Rathinam, Jules Lecomte, Jost Reelsen, Gregor Lenz, Axel von Arnim, Djamila Aouada"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04117v1"
tags: ["query:cislunar-dyn-nav"]
score: 8.0
evidence: 自主交会对接与近距离操作的航天器位姿估计
tldr: 针对航天器在极端光照和高速运动下的姿态估计难题，本文提出了一种结合事件相机与BrainChip Akida类脑处理器的6自由度姿态估计流水线。通过在SPADES数据集上训练轻量化MobileNet关键点回归网络，并利用量化感知训练将其转换为脉冲神经网络（SNN），实现了在Akida硬件上的低功耗、低延迟推理。这是首个在Akida硬件上运行的端到端航天器姿态估计演示，为未来自主空间任务提供了高效的感知方案。
selection_source: fresh_fetch
motivation: 传统相机在空间环境的极端光照和高速运动下易失效，需利用事件相机和类脑计算实现鲁棒且低功耗的姿态估计。
method: 采用轻量化MobileNet架构进行关键点回归，结合量化感知训练并将其部署为Akida兼容的脉冲神经网络。
result: 在Akida V1硬件上实现了实时、低功耗推理，并在Akida V2云端验证了基于热图的模型具有更高的姿态精度。
conclusion: 该研究证明了事件相机与类脑硬件结合在航天器自主感知任务中的实用性与高效性。
---

## 摘要
可靠的相对位姿估计是实现自主交会对接和近距离操作的关键，然而，由于极端的照明条件、高对比度和目标的快速运动，空间成像面临着众所周知的挑战。事件相机提供异步的、由变化驱动的测量，在基于帧的图像发生饱和或模糊时仍能保持信息丰富；而类脑处理器可以利用稀疏激活来实现低延迟、高能效的推理。本文提出了一种航天器六自由度（6-DoF）位姿估计流水线，将基于事件的视觉与 BrainChip Akida 类脑处理器相结合。利用 SPADES 数据集，我们在轻量级事件帧表示上训练了紧凑的 MobileNet 风格关键点回归网络，应用了量化感知训练（8/4位），并将模型转换为与 Akida 兼容的脉冲神经网络。我们对三种事件表示进行了基准测试，并在 Akida V1 硬件上演示了实时、低功耗的推理。此外，我们还针对 Akida V2 设计了一个基于热图的模型，并在 Akida 云端进行了评估，从而提高了位姿精度。据我们所知，这是首次在 Akida 硬件上运行航天器位姿估计的端到端演示，为未来自主空间任务实现低延迟、低功耗感知提供了一条切实可行的路径。

## Abstract
Reliable relative pose estimation is a key enabler for autonomous rendezvous and proximity operations, yet space imagery is notoriously challenging due to extreme illumination, high contrast, and fast target motion. Event cameras provide asynchronous, change-driven measurements that can remain informative when frame-based imagery saturates or blurs, while neuromorphic processors can exploit sparse activations for low-latency, energy-efficient inferences. This paper presents a spacecraft 6-DoF pose-estimation pipeline that couples event-based vision with the BrainChip Akida neuromorphic processor. Using the SPADES dataset, we train compact MobileNet-style keypoint regression networks on lightweight event-frame representations, apply quantization-aware training (8/4-bit), and convert the models to Akida-compatible spiking neural networks. We benchmark three event representations and demonstrate real-time, low-power inference on Akida V1 hardware. We additionally design a heatmap-based model targeting Akida V2 and evaluate it on Akida Cloud, yielding improved pose accuracy. To our knowledge, this is the first end-to-end demonstration of spacecraft pose estimation running on Akida hardware, highlighting a practical route to low-latency, low-power perception for future autonomous space missions.

---

## 论文详细总结（自动生成）

以下是对论文《Efficient Onboard Spacecraft Pose Estimation with Event Cameras and Neuromorphic Hardware》的结构化深入总结：

### 1. 核心问题与研究动机
*   **研究背景**：航天器在执行交会对接、碎片清理等近距离操作时，需要精确的 6-DoF（六自由度）相对位姿估计。
*   **核心挑战**：
    *   **极端环境**：空间环境存在极高对比度、强阴影、强镜面反射以及目标高速运动，传统帧相机易产生饱和或运动模糊。
    *   **资源受限**：航天器对功耗、体积和重量（SWaP）有极其严格的限制，传统 GPU 方案功耗过高。
*   **研究动机**：利用**事件相机**（高动态范围、无模糊、异步触发）和**类脑（Neuromorphic）处理器**（稀疏激活、低功耗、低延迟）的优势，构建一套适用于星载环境的高效感知流水线。

### 2. 方法论
论文提出了一种混合位姿估计流水线，核心步骤如下：
*   **事件表示（Event Representations）**：将异步事件流转换为适合 CNN/SNN 处理的张量，对比了三种轻量化方案：
    *   **E2F (Event-to-Frame)**：记录每个像素最后一次事件的极性。
    *   **2DHist (2D Histogram)**：累加时间窗口内的事件频率。
    *   **LNES (Locally-Normalised Event Surfaces)**：根据时间戳进行局部归一化，保留粗略的时间顺序。
*   **关键点回归网络**：
    *   **Akida V1 方案**：采用 MobileNet 风格架构，直接回归 8 个关键点的 2D 坐标（16 个标量）。
    *   **Akida V2 方案**：采用编码器-解码器架构，输出空间热图（Heatmap），将回归问题转化为空间分类问题。
*   **硬件适配与转换**：
    *   **量化感知训练 (QAT)**：针对 Akida V1 进行 4 位量化，针对 V2 进行 8 位量化。
    *   **SNN 转换**：利用 BrainChip 工具链将 CNN 转换为脉冲神经网络（SNN），利用硬件的稀疏性降低功耗。
*   **位姿解算**：通过 PnP（Perspective-n-Point）算法根据预测的 2D 关键点和已知 3D 模型恢复 6-DoF 位姿。

### 3. 实验设计
*   **数据集**：使用 **SPADES** 数据集，包含 179,400 帧合成事件数据（涵盖 300 条轨迹）。
*   **基准测试（Benchmark）**：
    *   **硬件对比**：BrainChip Akida AKD1000 (V1)、Akida Cloud (V2 模拟) 与 NVIDIA Jetson Orin Nano。
    *   **指标**：平移误差 ($E_T$)、旋转误差 ($E_R$)、综合位姿误差 ($E_P$)、关键点正确率 (PCK) 以及功耗/帧率。
*   **对比维度**：浮点模型 vs 量化模型、不同事件表示方案、V1 坐标回归 vs V2 热图回归。

### 4. 资源与算力
*   **训练算力**：文中提到使用了 **multi-GPU MirroredStrategy**（多 GPU 镜像策略）进行训练，但未指明具体的 GPU 型号（如 A100 或 RTX 3090）。
*   **训练时长**：Akida V1 模型训练了 300 个 Epoch 以确保坐标回归收敛；Akida V2 热图模型仅需 60 个 Epoch 即可达到 >0.96 的 PCK。
*   **推理算力**：Akida V1 运行在 AKD1000 SoC 上；V2 在云端模拟环境运行（频率模拟为 25MHz 和 400MHz）。

### 5. 实验数量与充分性
*   **实验规模**：测试集包含 35,889 帧数据。针对三种事件表示、两种硬件架构、浮点与量化状态进行了全交叉对比。
*   **充分性**：实验涵盖了从算法精度到硬件功耗、延迟的全面评估，并分析了物体距离对误差的影响。
*   **客观性**：通过与商用边缘 AI 平台（Jetson Orin Nano）对比，客观展示了类脑硬件在能效比上的优势。但实验主要基于合成数据，缺乏对 SPADES 真实实验数据的量化评估。

### 6. 主要结论
*   **能效优势**：Akida 硬件的动态功耗比 Jetson Orin Nano 低 **3 到 4 倍**。
*   **架构敏感性**：直接坐标回归（V1）对量化噪声极其敏感，4 位量化后精度大幅下降；而热图回归（V2）对量化具有极强的鲁棒性，8 位量化后精度几乎无损。
*   **表示方案**：E2F 和 2DHist 在 V1 上表现更稳健，而 LNES 在更高容量的 V2 架构中能提供更好的精度。
*   **实时性**：Akida V1 可实现 17 FPS，V2 在 400MHz 模拟频率下预计可达 37 FPS。

### 7. 优点
*   **首创性**：首次在 Akida 类脑硬件上实现了端到端的航天器位姿估计演示。
*   **硬件感知设计**：并非简单的模型移植，而是针对类脑芯片的稀疏性和量化特性优化了网络结构（如从坐标回归转向热图）。
*   **实用性强**：针对空间任务的 SWaP 限制，提供了一条低功耗、抗光照干扰的技术路径。

### 8. 不足与局限
*   **数据域偏差**：虽然使用了 SPADES 数据集，但评估仅限于**合成数据**，未在论文中展示真实实验室采集数据的测试结果，实际部署时的领域泛化性有待验证。
*   **接口瓶颈**：目前 Akida V1 的推理速度（17 FPS）受限于主机接口通信，未能完全发挥类脑芯片的超低延迟潜力。
*   **量化损失**：V1 架构下的 4 位量化导致精度下降严重，限制了第一代硬件在高性能任务中的应用。

（完）
