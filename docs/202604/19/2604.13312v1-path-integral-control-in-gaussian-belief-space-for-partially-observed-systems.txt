Title: Path Integral Control in Gaussian Belief Space for Partially Observed Systems

URL Source: https://arxiv.org/pdf/2604.13312v1

Published Time: Thu, 16 Apr 2026 00:14:56 GMT

Number of Pages: 6

Markdown Content:
# Path Integral Control in Gaussian Belief Space for Partially Observed Systems 

Goutam Das 1, Takashi Tanaka 2

Abstract — This paper extends path integral control (PIC) to partially observed systems by formulating the problem in Gaussian belief space. PIC relies on the diffusion being proportional to the control channel—the so-called matching condition—to linearize the Hamilton–Jacobi–Bellman equation via the Cole–Hopf transform; we show that this condition fails in infinite-dimensional belief space under non-affine observa-tions. Restricting to Gaussian beliefs yields a finite-dimensional approximation with deterministic covariance evolution, reduc-ing the problem to stochastic control of the belief mean. We derive necessary and sufficient conditions for matching in this reduced space, obtain an exact Cole–Hopf linearization with a Feynman–Kac representation, and develop the MPPI-Belief algorithm. Numerical experiments on a navigation task with state-dependent observation noise demonstrate the effectiveness of MPPI-Belief relative to certainty-equivalent and particle-filter-based baselines. 

I. I NTRODUCTION 

Stochastic optimal control under partial observability is a central problem in robotics, autonomous navigation, and aerospace systems [1]–[3]. When the state cannot be mea-sured directly, the controller must reason over a belief —a probability distribution over possible states—and plan in belief space, leading to partially observable Markov deci-sion processes (POMDPs) that are generally intractable in continuous state, action, and observation spaces [4], [5]. A powerful class of methods for fully observed stochastic control is path integral control (PIC), introduced by Kap-pen [6] and closely related to linearly-solvable MDPs [7]. PIC relies on a restrictive condition—the matching condi-tion —requiring that the noise covariance HH ⊤ be propor-tional to the control authority G(x) R−1G(x)⊤. When this holds, the nonlinear HJB equation can be linearized via the Cole–Hopf transform, yielding a Feynman–Kac representa-tion and enabling Monte Carlo solution. This principle under-lies model predictive path integral control (MPPI) [8] and has been extended to state-dependent diffusion [9], constrained formulations [10], and risk-sensitive variants [11]. All of these methods, however, rely on full state observation. Under partial observability, the natural state is no longer the physical state but the belief. This raises a critical ques-tion that has received limited attention: can the matching condition required for Cole–Hopf linearization be satisfied in belief space? Existing approaches to partially observed MPPI are primarily implementation-driven and do not analyze                 

> This work is supported by DARPA COMPASS program grant HR0011-25-3-0210 and AFOSR DSCT program grant FA9550-25-1-0347. All authors are associated with the Networked Control Systems Lab at Purdue University. Emails: 1das347@purdue.edu, and 2
> tanaka16@purdue.edu.

whether the matching condition extends to belief space. Mohamed et al. [12] apply MPPI with local costmaps for navigation in unknown cluttered environments; Abraham et al. [13] extend path integral control to learned uncer-tain dynamics; and Hoshino et al. [14] combine a particle filter with per-particle path integral controllers. In parallel, belief-space planning methods such as LQG-MP [15] and maximum-likelihood belief planning [4] exploit Gaussian structure for trajectory optimization, but do not connect this structure to the PIC matching and linearization framework. Thus, while filtering-based and particle-based approxima-tions exist, whether the matching condition underlying PIC can be satisfied in belief space remains an open question. This paper addresses this gap by extending PIC to Gaus-sian belief space and analyzing its validity under partial observability. The contributions are as follows: (i) We show that the PIC matching condition in belief space requires affine observations, establishing a general impossibility result for nonlinear observation models. (ii) Under a Gaussian ap-proximation, the belief covariance evolves deterministically, reducing the problem to stochastic control of the belief mean. We derive necessary and sufficient range-space conditions under which the matching condition holds in this reduced space, leading to exact Cole–Hopf linearization. (iii) We obtain a Feynman–Kac representation yielding the MPPI-Belief controller (Algorithm 1) and introduce a risk-sensitive cost extension. The paper is organized as follows. Section II formulates the partially observed control problem. Section III derives the belief dynamics and shows that the matching condition cannot be satisfied in full belief space under non-affine observations. Section IV develops a Gaussian belief-space re-duction and distinguishes the exact and approximate regimes. Section V presents the main theoretical results. Section VI presents the MPPI-Belief algorithm, and Section VII pro-vides numerical validation. 

A. Notation 

We write range( M ) and rank( M ) for the range and rank of a matrix M , respectively; Sn 

> +

and Sn 

> ++

for the cones of n × n positive semidefinite (PSD) and positive definite (SPD) matrices; M † for the Moore–Penrose pseudoinverse; and N (μ, Σ) for the Gaussian distribution with mean μ and covariance Σ.II. P ROBLEM FORMULATION 

We consider a control-affine stochastic system in continuous time and continuous space, where the state is measured 

> arXiv:2604.13312v1 [eess.SY] 14 Apr 2026

through a noisy output. The state and observation equations are described by the stochastic differential equations 

dx t = f (xt) dt + G(xt) ut dt + H dw t, (1a) 

dy t = c(xt) dt + σo dν t, (1b) where xt ∈ Rn is the state, ut ∈ Rℓ is the control input, and yt ∈ Rp is the observation. The function f : Rn → Rn

describes the uncontrolled drift, G : Rn → Rn×ℓ is the control input matrix, and c : Rn → Rp is the observation function. The matrix H ∈ Rn×m determines the process noise channel, and σo ∈ Rp×p is the observation noise matrix, assumed to be invertible. The Brownian motions 

wt ∈ Rm and νt ∈ Rp are mutually independent. We define the process noise covariance Q := HH ⊤ ∈ Sn 

> +

and the observation noise covariance Ro := σoσ⊤ 

> o

∈ Sp

> ++

.Let Fyt := σ{ys : 0 ≤ s ≤ t} denote the σ-algebra generated by the observation history up to time t. An ad-missible control is an Fyt -adapted, square-integrable process 

{ut}t∈[0 ,T ]. This means that the control at time t is deter-mined based on the history of noisy observations {ys}0≤s≤t,rather than the system state xt. The cost functional is given by 

J = E

"

¯ϕ(xT ) + 

Z T

> 0



¯q(xs) + 12 u⊤ 

> s

R u s



ds 

#

, (2) where ¯ϕ : Rn → R and ¯q : Rn → R are the terminal and running state costs, assumed to be non-negative, and 

R ∈ Sℓ 

> ++

is the control cost weight. The partially observed control problem is formulated as follows. 

Problem 1. Consider the state and observation equa-tions (1a) –(1b) and the cost functional (2) . Find an admis-sible control {ut}t∈[0 ,T ] that minimizes the cost (2) , given only the observation history {ys}0≤s≤t.

This problem is more challenging than the fully observed counterpart, since the controller cannot access the state xt di-rectly and must instead reason over a probability distribution conditioned on the observation history. Since xt is not directly accessible, the cost must be expressed in terms of the conditional distribution of the state given the observations, commonly referred to as the 

belief [16]. The belief-space formulation applies to general partially observed systems and does not require Gaussian-ity. The Gaussian restriction introduced in Section IV is a computational choice that enables the finite-dimensional reduction used in the path integral construction below. Under a Gaussian belief πt = N (μt, Σt), we define the expected running cost 

q(μ, Σ) := Ex∼N (μ, Σ) [¯ q(x)] , (3) and the analogous terminal cost ϕ(μ, Σ) . For quadratic 

¯q(x) = 12 ∥x − xref ∥2 

> Qx

, this yields 

q(μ, Σ) = 12 ∥μ − xref ∥2 

> Qx

+ 12 tr( QxΣ) . (4) Thus, the expected cost decomposes into a term evaluated at the belief mean and a term that penalizes state uncertainty through the covariance. Dropping the trace term recovers the certainty-equivalent approach of planning on the mean alone, which underestimates the true expected cost whenever Σ is non-trivial. 

Remark 1 (Classical PIC Matching) . For the fully observed system (1a) with cost (2) , the matching condition requires 

Q = λ G (x) R−1G(x)⊤ for some λ > 0. This means that the process noise and the control input act through the same subspace of the state space. When matching holds, the associated HJB equation admits a Cole–Hopf linearization, which yields a linear PDE with a Feynman–Kac represen-tation and enables Monte Carlo solution. Without matching, this linearization is not available. This structure underlies MPPI [8]; when matching is only approximately satisfied, practical strategies such as importance-sampling correc-tions [17] and modified cost formulations [12] have been proposed. 

III. B ELIEF DYNAMICS AND MATCHING IMPOSSIBILITY 

This section derives the belief dynamics for the partially observed system (1a)–(1b) and analyzes the structure of the resulting control and diffusion channels in belief space. Since the state xt is not directly accessible, the controller must reason over the conditional distribution πt(·) := P(xt ∈· | F yt ), which is a measure-valued stochastic process. Its evolution is governed by the Kushner–Stratonovich equation (KSE) [18]: 

dπ t(x) = ( Lu∗ 

> t

πt)( x) dt +πt(x)  c(x)−πt(c)⊤R−1 

> o

d˜yt,

(5) where Lu∗ 

> t

is the Fokker–Planck operator associated with the controlled dynamics (1a), and d˜yt := dy t − πt(c) dt is the innovation process , whose whitened form R−1/2 

> o

d˜yt is a standard Fyt -Brownian motion (see [19]). To examine whether the structure underlying path integral control in the fully observed setting (Remark 1) extends to belief space, we rewrite the belief dynamics (5) in the form 

dπ = au(π) dt + b(π) d˜y. In particular, this yields 

au(π)( x) = a0(π)( x) − ∇ ·  G(x) u π (x), (6a) 

b(π)( x) = π(x)  c(x) − π(c)⊤R−1 

> o

. (6b) Thus, (6a) shows that the drift depends on the control through the divergence term −∇· (G(x) u π (x)) , whereas (6b) shows that the diffusion is independent of u.In the fully observed setting (Remark 1), matching re-quires that the diffusion and control channels be aligned. An analogous condition in belief space would require that the observation-driven diffusion (6b) be proportional to the control-induced perturbation (6a). If such a condition held, the corresponding belief-space HJB equation would admit a Cole–Hopf linearization, yielding a Feynman–Kac represen-tation and enabling sampling-based control, as in the fully observed case. The following result characterizes when this is possible. Theorem 1 (Affine Observation Characterization) . Consider the system (1a) –(1b) with constant G ∈ Rn×ℓ and observa-tion function c : Rn → Rp. Restrict the belief to the Gaussian family {πμ = N (μ, Σ) : μ ∈ Rn} with fixed Σ ≻ 0. Then the observation-driven diffusion lies in the tangent space of this Gaussian mean manifold if and only if 

c(x) − Eπμ [c(x)] = A(μ)( x − μ) for all μ ∈ Rn, (7) 

for some matrix-valued function A(μ) ∈ Rp×n. Moreover, this holds if and only if c is affine: c(x) = Ax + b for some constant matrix A ∈ Rp×n and vector b ∈ Rp.Proof. Under the Gaussian family πμ = N (μ, Σ) with fixed Σ ≻ 0, the mean-tangent direction is proportional to Σ−1(x − μ) πμ(x), while the observation-induced dif-fusion (6b) is proportional to πμ(x) c(x) − ¯c(μ), where 

¯c(μ) := Eπμ [c(x)] . Thus, the observation-driven diffusion lies in the Gaussian mean tangent space if and only if 

c(x) − ¯c(μ) = A(μ)( x − μ) for some A(μ) ∈ Rp×n.Evaluating at μ1̸ = μ2 and subtracting: 

¯c(μ2) − ¯c(μ1) = A(μ1) − A(μ2) x − A(μ1)μ1 + A(μ2)μ2.

The left-hand side is independent of x, so A(μ1) = A(μ2).Since μ1, μ 2 are arbitrary, A(μ) ≡ A is constant. Substituting back gives c(x) = Ax + (¯ c(μ) − Aμ ); since the left-hand side is independent of μ, the quantity ¯c(μ) − Aμ is constant, yielding c(x) = Ax + b. The converse is immediate. □

The restriction to fixed Σ in Theorem 1 isolates the role of the observation function c; the time-varying covariance case is addressed in Section IV, where matching feasi-bility is analyzed for the resulting time-varying diffusion 

Dt (Proposition 3). For state-dependent G(x), the same difficulty persists, as the observation and control channels are generically misaligned. 

Remark 2. In particular, every scalar linear-Gaussian sys-tem ( c(x) = ax +b, G̸ = 0 ) satisfies Theorem 1 and, provided 

Dt̸ = 0 , also satisfies the range condition of Proposition 3. Thus, belief-space PIC applies exactly in this case. 

This motivates restricting the belief to a finite-dimensional family of distributions. IV. G AUSSIAN BELIEF -S PACE APPROXIMATION 

In this section, we focus on the Gaussian family πt =

N (μt, Σt) and derive the resulting mean and covariance dynamics. We state the following classical result explicitly, as the deterministic evolution of Σt is the property that enables the belief-space PIC reduction developed in Section V. 

Proposition 2 (Deterministic Covariance: Linear Case) . For the linear system f (x) = Ax , G(x) = B, and c(x) = Cx 

in (1a) –(1b) , the conditional distribution πt = N (μt, Σt) is exactly Gaussian, with 

dμ t = ( Aμ t + Bu t) dt + Kt d˜yt, (8a) 

˙Σt = AΣt + Σ tA⊤ + Q − ΣtC⊤R−1 

> o

CΣt, (8b) 

where Kt := Σ tC⊤R−1 

> o

is the Kalman gain. The innovation process in (5) specializes to d˜yt = dy t −Cμ t dt in the linear-Gaussian case, since πt(c) = Cμ t. The Riccati equation (8b) 

is a deterministic ODE, independent of both the observations and the control input. Proof. The exact Gaussianity of πt and the dynamics (8) follow from Kalman–Bucy filtering theory [19]. The Riccati equation (8b) is deterministic by construction. □

A key consequence of Proposition 2 is that the stochastic component of the mean dynamics is driven entirely by the innovation process, with effective covariance 

Dt := Kt Ro K⊤ 

> t

= Σ tC⊤R−1 

> o

CΣt. (9) The matrix Dt quantifies the strength of the innovation-driven fluctuations in the belief mean: larger values of Dt

indicate that new observations have a stronger effect on the state estimate, whereas Dt = 0 implies that the belief mean is not updated by the observations. Let rt := rank( Dt) and let Lt ∈ Rn×rt satisfy Dt = LtL⊤ 

> t

.Since the quadratic variation of Kt d˜yt is Dt dt , factoring 

Dt = LtL⊤ 

> t

and defining βt through the innovation process allow the mean dynamics (8a) to be written as 

dμ t = ( Aμ t + Bu t) dt + Lt dβ t, (10) where βt ∈ Rrt is a standard Brownian motion adapted to Fyt . Since Σt is deterministic, both Dt and Lt are deterministic functions of time, so (10) is a standard SDE driven by the innovation Brownian motion. 

Remark 3 (Nonlinear Case: EKF Approximation) . For non-linear systems (1a) –(1b) , linearizing f and c about a nominal trajectory ¯μt with At := ∂f /∂x |¯μt and Ct := ∂c/∂x |¯μt

yields the EKF analogue of (8b) :

˙Σt = AtΣt + Σ tA⊤ 

> t

+ Q − ΣtC⊤ 

> t

R−1 

> o

CtΣt, (11) 

which is a deterministic ODE once ¯μt is specified. This follows from a first-order Taylor expansion of the drift and observation functions, under which the KSE reduces formally to the Kalman–Bucy form (8) with time-varying Jacobians At

and Ct (see, e.g., [19]). The neglected residual is formally second-order in ∥μt − ¯μt∥, with constants depending on the local curvature of f and c; a rigorous bound is beyond the scope of this paper. 

Assumption 1 (Deterministic Gain Schedule) . Over [0 , T ],the quantities Kt, Σt, and Dt := KtRoK⊤ 

> t

are assumed to be predetermined deterministic functions of time, obtained by solving (8b) in the linear case or (11) in the nonlinear case about a nominal trajectory. For nonlinear systems, we write ¯Gt := G(¯ μt). For linear systems, ¯Gt = B for all t.

Under Assumption 1, the belief-mean dynamics are ap-proximated by 

dμ t =  f (μt) + ¯Gt ut

 dt + Lt dβ t, (12) where Dt = LtL⊤ 

> t

is deterministic. Since Σt is a known function of time, the covariance contribution to the cost becomes a deterministic time-dependent term. Accordingly, we define the reduced running cost qt(μ) := q(μ, Σ( t)) and the reduced terminal cost ϕT (μ) := ϕ(μ, Σ( T )) .V. P ATH INTEGRAL CONTROL IN GAUSSIAN BELIEF 

SPACE 

This section develops the main theoretical results in Gaussian belief space: matching feasibility, Cole–Hopf linearization, and the Feynman–Kac representation. While the Cole–Hopf and Feynman–Kac machinery is classical in fully observed PIC, the present setting is different because the reduced belief-mean dynamics (12) are driven by the diffusion Dt

induced by the observation channel rather than by the pro-cess noise. Accordingly, the belief-space matching condition involves the observation-driven diffusion Dt rather than the process noise Q, and its feasibility is characterized by Proposition 3. 

A. HJB Equation for the Belief Mean 

Since Σt is deterministic under Assumption 1, the stochastic optimal control problem for the belief-mean dynamics (12) under the reduced costs qt and ϕT reduces to a standard finite-dimensional problem. Let V (t, μ ) denote the corre-sponding optimal cost-to-go, which satisfies the HJB equa-tion: 

− ∂tV = min 

> u



qt(μ) + 12 u⊤R u 

+ ( ∇μV )⊤ f (μ) + ¯Gt u + 12 tr( Dt ∇2

> μμ

V )



(13) with V (T, μ ) = ϕT (μ). First-order optimality yields u∗ =

−R−1 ¯G⊤ 

> t

∇μV , and substitution into (13) gives the mini-mized HJB: 

− ∂tV = qt(μ) + ( ∇μV )⊤f (μ) + 12 tr( Dt ∇2

> μμ

V )

− 12 (∇μV )⊤ ¯GtR−1 ¯G⊤ 

> t

∇μV. (14) The quadratic gradient term in (14) renders the equation non-linear. The next subsection identifies conditions under which this term can be removed by the Cole–Hopf transformation. 

B. Matching Condition and Feasibility 

In the fully observed setting (Remark 1), the Cole–Hopf transformation requires that the diffusion covariance equal a scaled version of the control authority matrix GR −1G⊤.We now state the analogous requirement for the belief-mean dynamics (12). Given this condition, the Cole–Hopf linearization follows by the same algebraic mechanism as in the fully observed case. The non-trivial question is whether and when this condition can be satisfied in belief space; this is addressed by Proposition 3 below. 

Assumption 2 (Scheduled Matching) . The belief-mean dif-fusion matrix Dt (9) and the scheduled control matrix ¯Gt

satisfy, for each t ∈ [0 , T ],

Dt = λ ¯Gt R−1 ¯G⊤ 

> t

, (15) 

where λ > 0 is the path integral temperature. 

Proposition 3 (Matching Feasibility) . For each t ∈ [0 , T ]:

(i) There exists a symmetric matrix W ⪰ 0 such that Dt =

λ ¯GtW ¯G⊤ 

> t

if and only if range( Dt) ⊆ range( ¯Gt).

(ii) There exists R ≻ 0 satisfying (15) if and only if 

range( Dt) = range( ¯Gt).Proof. For part (i), note that range( ¯GtW ¯G⊤ 

> t

) ⊆ range( ¯Gt)

for any W , so the range inclusion is necessary. Conversely, if range( Dt) ⊆ range( ¯Gt), then D1/2 

> t

admits a factorization 

D1/2 

> t

= ¯GtM for some matrix M , and setting W =

M M ⊤/λ yields the result. For part (ii), if R ≻ 0 then R−1 ≻ 0, so 

range( ¯GtR−1 ¯G⊤ 

> t

) = range( ¯Gt), which forces range equal-ity.Conversely, given range( Dt) = range( ¯Gt), one can construct an SPD matrix R−1 by solving on range( ¯Gt) via the SVD and extending to a full-rank matrix using a positive multiple of the projector onto ker( ¯Gt). □

Here R ≻ 0 is the fixed control cost weight from (2). Since Dt and ¯Gt vary with t, the condition (15) is verified pointwise; W in part (i) is an auxiliary existence variable, not a design parameter. 

C. Cole–Hopf Linearization 

Define the desirability function Ψ( t, μ ) := exp( −V (t, μ )/λ ), so that V (t, μ ) = −λ log Ψ( t, μ ).

Theorem 4 (Exact Linearization via Cole–Hopf) . Under As-sumptions 1 and 2, the substitution V (t, μ ) = −λ log Ψ( t, μ )

converts the HJB equation (14) into the linear PDE 

−∂tΨ = − qt(μ)

λ Ψ+( ∇μΨ) ⊤f (μ)+ 12 tr( Dt ∇2

> μμ

Ψ) , (16) 

with terminal condition, Ψ( T, μ ) = exp( −ϕT (μ)/λ ).

Proof. From V = −λ log Ψ , direct computation gives 

∇μV = − λ

Ψ ∇μΨ,

∇2

> μμ

V = − λ

Ψ ∇2

> μμ

Ψ + λ

Ψ2 (∇μΨ)( ∇μΨ) ⊤.

Substituting into (14), the Hessian trace produces 

(λ/ 2Ψ 2)( ∇μΨ) ⊤Dt∇μΨ, while the quadratic control term yields −(λ2/2Ψ 2)( ∇μΨ) ⊤ ¯GtR−1 ¯G⊤ 

> t

∇μΨ. Under the matching condition (15), these cancel exactly, giving (16). □

Since (16) is linear, the desirability admits the following stochastic representation. 

Corollary 5 (Belief-Space Feynman–Kac Representation) .

Under Assumptions 1 and 2, let Q denote the probability measure corresponding to the uncontrolled belief-mean dy-namics. Then 

Ψ( t, μ ) = EQ



exp 



− 1

λ

Z Tt

qs(μs) ds − ϕT (μT )

λ



μt = μ



.

(17) This expresses the desirability as an expectation over uncontrolled belief-mean trajectories, enabling Monte Carlo approximation. Remark 4 (Risk-Sensitive Extension) . The expected cost 

q(μ, Σ) = E[¯ q(x)] can be replaced by qRS = E[¯ q(x)] +  

> θ
> 2

Var[¯ q(x)] , obtained from the cumulant expansion of the entropic risk measure [20], [21]. The parameter θ > 0

controls risk aversion, and θ = 0 recovers the risk-neutral cost. The PIC structure is preserved since only the running cost is modified. 

VI. MPPI-B ELIEF ALGORITHM 

This section develops a discrete-time Monte Carlo approx-imation of the belief-space control law and presents the resulting MPPI-Belief algorithm. Under an Euler–Maruyama discretization of the uncon-trolled belief-mean dynamics (12) with ut ≡ 0 at times 

tk = k∆t, the transitions are 

μk+1 = μk + f (μk) ∆ t + Lk ϵk, ϵk ∼ N (0 , I rk ∆t), (18) where LkL⊤ 

> k

= Dtk and rk := rank( Dtk ).

Theorem 6 (Discrete-Time Control Law) . Under Assump-tions 1–2, and freezing the coefficients over the first time step (standard in MPPI [8]), the control law at step k = 0 is 

u∗ 

> 0

=

> N

X

> i=1

˜w(i) R−1 ¯G⊤ 

> 0

( ¯G0R−1 ¯G⊤ 

> 0

)† L0 ϵ(i)0

∆t , (19) 

where ¯G0 := G(¯ μ0), the normalized importance weights are ˜w(i) := w(i)/ P 

> j

w(j) with w(i) := exp( −S(i)/λ ), and 

S(i) is the path cost of trajectory i. For rank-deficient D0,the pseudoinverse restricts the score to range( D0), ensuring well-posedness for underactuated systems. Proof. From the first-order optimality condition u∗ =

−R−1 ¯G⊤ 

> t

∇μV and ∇μV = −(λ/ Ψ) ∇μΨ,

u∗ = λR −1 ¯G⊤

> t

∇μΨΨ .

By the Feynman–Kac representation (Corollary 5), the quan-tity ∇μ0 log Ψ can be written as a cost-weighted expectation of the score function of the uncontrolled transition density. For the discrete transition (18) with covariance D0∆t, the score restricted to range( D0) is D†

> 0

L0ϵ0/∆t. Substituting the matching condition D0 = λ ¯G0R−1 ¯G⊤ 

> 0

gives 

λR −1 ¯G⊤ 

> 0

D† 

> 0

= R−1 ¯G⊤ 

> 0

( ¯G0R−1 ¯G⊤ 

> 0

)†.

Approximating the resulting expectation with N Monte Carlo samples yields (19). □

The complete MPPI-Belief algorithm is given in Algo-rithm 1. In practice, Algorithm 1 is applied in a receding-horizon fashion: at each time step, the belief (μ0, Σ0) is updated from the latest filter estimate, and only the control u∗ 

> 0

is applied before replanning. In contrast to particle-filter-based methods such as PIPF [14], which combine filtering particles with per-particle path integral sampling, MPPI-Belief operates on a single belief-mean trajectory with N samples. 

Algorithm 1 MPPI-Belief 

Require: Belief (μ0, Σ0), number of samples N , horizon length H = T / ∆t, step size ∆t, temperature λ 

> 1:

Precompute {Σk, K k, D k, L k}Hk=0 by solving the Riccati equation along the nominal trajectory ¯μ 

> 2:

¯G0 ← G(¯ μ0) 

> 3:

M0 ← R−1 ¯G⊤ 

> 0

( ¯G0R−1 ¯G⊤ 

> 0

)†L0 

> 4:

for i = 1 , . . . , N do  

> 5:

μ(i)0 ← μ0, S(i) ← 0 

> 6:

for k = 0 , . . . , H − 1 do  

> 7:

Sample ϵ(i) 

> k

∼ N (0 , I rk ∆t) 

> 8:

μ(i) 

> k+1

← μ(i) 

> k

+ f (μ(i) 

> k

)∆ t + Lkϵ(i) 

> k
> 9:

S(i) ← S(i) + qtk (μ(i) 

> k

)∆ t 

> 10:

end for  

> 11:

S(i) ← S(i) + ϕT (μ(i) 

> H

) 

> 12:

w(i) ← exp( −S(i)/λ ) 

> 13:

end for  

> 14:

Normalize: ˜w(i) ← w(i)/ P 

> j

w(j) 

> 15:

return u∗ 

> 0

= M0

P 

> i

˜w(i)ϵ(i)0 /∆t

VII. N UMERICAL VALIDATION 

We evaluate on a 2D double-integrator with state x =[px, p y , v x, v y ]⊤, control u ∈ R2, and position-only obser-vations ( C = [ I2 02×2]). The observation noise is σo(px) = 

|px − xlight |/√2 + 0 .1 with xlight = 5 m, producing a light region where observations are most accurate. Two obstacles at (3 , ±1) m with radius 0.65 m form a corridor in a high-noise region; process noise is σw = 0 .30 . All sampling-based methods use N =500 samples, H=30 , ∆t=0 .1 s, and λ=1 .We compare MPPI-Belief with the risk-sensitive cost of Remark 4 (θ=1 ) against EKF-iLQG [22], CE-MPPI (θ=0 ), and PIPF [14] ( K=50 particles, L=200 samples). All sampling-based baselines share the same cost function, temperature, and horizon, while differing primarily in plan-ning and filtering architecture. 1

Table I and Fig. 1 show that MPPI-Belief is the only method achieving zero collisions over 200 trials, while the baselines incur 18–25%. The risk-sensitive belief cost cou-ples obstacle proximity with covariance, encouraging detours through the informative region before traversing the corridor.   

> 1Our PIPF uses a bootstrap particle filter and the same MPPI importance weighting as CE-MPPI, rather than the KL-optimal proposal of [14]; thus, our implementation should be viewed as a simplified PIPF-style baseline rather than an exact reproduction. TABLE I COMPARATIVE PERFORMANCE OF FOUR CONTROLLERS ON THE GRADIENT LIGHT -DARK DOMAIN (200 M ONTE CARLO TRIALS ).

Method Cost Collision Time (ms) EKF-iLQG 46 .1 ± 6.2 24.5% 148 CE-MPPI 50 .5 ± 6.2 23.0% 13 PIPF 47 .6 ± 5.5 18.0% 296 MPPI-Belief 56 .5 ± 5.2 0.0% 18 0 2 4 6 8 10  

> px(m)
> −2
> −1
> 0
> 1
> 2
> py (m)
> Light
> EKF-iLQG
> CE-MPPI
> PIPF
> MPPI-Belief
> Obstacle
> Start
> Goal
> 0.25
> 0.50
> 0.75
> 1.00
> 1.25
> 1.50
> 1.75
> 2.00
> σo(px)

Fig. 1. Sample trajectories (50 per method) on the gradient light-dark domain. Background shading indicates observation noise σo(px); the light region near px=5 m provides accurate observations. 10 2 10 3 10 4    

> Planning obstacle weight wobs
> 0
> 5
> 10
> 15
> 20
> 25
> 30
> 35
> Collision rate (%)
> (a)
> EKF-iLQG
> CE-MPPI
> PIPF
> MPPI-Belief
> 10 210 310 4
> Planning obstacle weight wobs
> 35
> 40
> 45
> 50
> 55
> 60
> Base cost (no obstacle penalty)
> (b)
> EKF-iLQG
> CE-MPPI
> PIPF
> MPPI-Belief

Fig. 2. Obstacle-weight sweep (200 trials per point). (a) Collision rate versus wobs . (b) Base cost excluding obstacle penalties. MPPI-Belief attains zero collisions at sufficiently large wobs , unlike the baselines. 

The optimized matching residual is ε∗ = 0 .79 , indicating that useful performance is retained despite approximate matching. In Fig. 2, we sweep the planning obstacle penalty weight 

wobs ∈ [10 2, 10 4] for each method to test whether the safety improvement is merely a tuning artifact. The results show that increasing wobs across two orders of magnitude has little effect on the baselines, which remain between 11% and 33% collision rate, whereas MPPI-Belief drops below 1% by wobs =500 and reaches 0% at 2500. No baseline reaches 0% at any weight tested, indicating that collisions are driven by estimation uncertainty rather than insufficient obstacle penalty. Finally, we isolate the role of the risk-sensitive parameter by sweeping θ ∈ { 0, 0.5, 1, 2, 5} over 200 trials per setting (detailed results omitted for brevity). At θ=0 (expected cost only), the collision rate is 23.0% with mean clearance 0.11 m. Even moderate risk aversion ( θ=0 .5) reduces collisions to 2.5% and increases clearance to 0.80 m. The best perfor-mance occurs at θ=1 , achieving 0% collisions and 0.82 m clearance with modest cost increase ( 56 .6 vs. 51 .3). For 

θ ≥ 2, collision rates rise slightly (0.5–1.5%), suggesting that excessive risk aversion can induce over-conservatism. The variance penalty (θ/ 2)Var[¯ q] is thus the primary driver of safe behavior, amplifying obstacle costs in high-uncertainty regions. VIII. C ONCLUSION 

This paper studied path integral control for partially ob-served systems via a Gaussian belief-space approximation. We showed that the matching condition generally fails in full belief space, and characterized when it can hold in the reduced Gaussian setting. This yields a belief-space path in-tegral formulation and the MPPI-Belief algorithm. Numerical experiments showed improved safety relative to certainty-equivalent and particle-filter-based baselines at practical computational cost. Future work will consider nonlinear observation models, non-Gaussian belief representations, and hardware validation. REFERENCES [1] K. Oguri, “Chance-constrained control for safe spacecraft autonomy: Convex programming approach,” in 2024 Amer. Control Conf. (ACC) ,2024, pp. 2318–2324. [2] M. Dor, T. Driver, K. Getzandanner, and P. Tsiotras, “Astroslam: Autonomous monocular navigation in the vicinity of a celestial small body—theory and experiments,” The Int. J. of Robotics Res. , vol. 43, no. 11, pp. 1770–1808, 2024. [3] M. Lauri, D. Hsu, and J. Pajarinen, “Partially observable markov decision processes in robotics: A survey,” IEEE Trans. on Robotics ,vol. 39, no. 1, pp. 21–40, 2022. [4] R. Platt Jr, R. Tedrake, L. Kaelbling, and T. Lozano-Perez, “Be-lief space planning assuming maximum likelihood observations,” in 

Robot.: Sci. Syst. (RSS) , 2010. [5] Z. Sunberg and M. Kochenderfer, “Online algorithms for POMDPs with continuous state, action, and observation spaces,” in Proc. Int. Conf. Autom. Plan. Scheduling (ICAPS) , vol. 28, 2018, pp. 259–263. [6] H. J. Kappen, “Path integrals and symmetry breaking for optimal control theory,” J. Stat. Mech.: Theory Exp. , vol. 2005, no. 11, p. P11011, 2005. [7] E. Todorov, “Linearly-solvable Markov decision problems,” Adv. Neu-ral Inf. Process. Syst. , vol. 19, 2006. [8] G. Williams, N. Wagener, B. Goldfain, P. Drews, J. M. Rehg, B. Boots, and E. A. Theodorou, “Information theoretic MPC for model-based reinforcement learning,” in Proc. IEEE Int. Conf. Robot. Autom. (ICRA) , 2017, pp. 1714–1721. [9] E. Theodorou, J. Buchli, and S. Schaal, “A generalized path integral control approach to reinforcement learning,” J. Mach. Learn. Res. ,vol. 11, pp. 3137–3181, 2010. [10] M. S. Gandhi, B. Vlahov, J. Gibson, G. Williams, and E. A. Theodorou, “Robust model predictive path integral control: Analysis and performance guarantees,” IEEE Robot. Autom. Lett. , vol. 6, no. 2, pp. 1423–1430, 2021. [11] J. Yin, A. Iyer, and E. A. Theodorou, “Risk-aware model predictive path integral control,” in Proc. Amer. Control Conf. (ACC) , 2023, pp. 3467–3474. [12] I. S. Mohamed, K. Yin, and L. Liu, “Autonomous navigation of AGVs in unknown cluttered environments: Log-MPPI control strategy,” IEEE Robot. Autom. Lett. , vol. 7, no. 4, pp. 10 240–10 247, 2022. [13] I. Abraham, A. Handa, N. Ratliff, K. Lowrey, T. D. Murphey, and D. Fox, “Model-based generalization under parameter uncertainty using path integral control,” IEEE Robot. Autom. Lett. , vol. 5, no. 2, pp. 2864–2871, 2020. [14] K. Hoshino, H. Yu, T. Tanaka, and Y. Chen, “Path integral control of partially observed systems via fully observable control approxima-tions,” Syst. Control Lett. , vol. 204, p. 106185, 2025. [15] J. Van Den Berg, S. Patil, and R. Alterovitz, “Motion planning under uncertainty using iterative local optimization in belief space,” Int. J. Robot. Res. , vol. 31, no. 11, pp. 1263–1278, 2012. [16] V. Krishnamurthy, Partially Observed Markov Decision Processes .Cambridge Univ. Press, 2016. [17] G. Williams, B. Goldfain, P. Drews, K. Saigol, J. M. Rehg, and E. A. Theodorou, “Robust sampling based model predictive control with sparse objective information,” in Robot.: Sci. Syst. (RSS) , vol. 14, 2018. [18] H. J. Kushner, “Dynamical equations for optimal nonlinear filtering,” 

J. Differ. Equ. , vol. 3, no. 2, pp. 179–190, 1967. [19] R. S. Liptser and A. N. Shiryaev, Statistics of Random Processes: General Theory . Springer, 1977, vol. 394. [20] D. Jacobson, “Optimal stochastic linear systems with exponential performance criteria and their relation to deterministic differential games,” IEEE Trans. Autom. Control , vol. 18, no. 2, pp. 124–131, 1973. [21] P. Whittle, Risk-Sensitive Optimal Control . Wiley, 1990. [22] E. Todorov and W. Li, “A generalized iterative LQG method for locally-optimal feedback control of constrained nonlinear stochastic systems,” in Proc. Amer. Control Conf. (ACC) , 2005, pp. 300–306.